# Contur3 — Ireland Manual Command Pack (READ-ONLY by default)

Generated by the canonical live-funnel monitor. This pack is for the operator on
the Ireland executor host. It is **fail-closed / dry-run by default**.

## Hard safety rules (non-negotiable)

- **Do NOT run the old unsafe `live/night_live_loop.py` as a production executor.**
- **Live order placement requires explicit founder approval.** No command here
  places a live order.
- Queue monitor / readiness checks may run **only** if the script supports
  `--dry-run` / fail-closed mode.
- Ireland never decides strategy. PREMVP decides candidate pool, tiers, fallback,
  stake sizes. Ireland validates and executes/fail-closes only.
- Forbidden markets must never be executable anchors (halftime/1H/2H, corners,
  exact score, goalscorer, player props, futures/outrights).

## 1. Connect

```
# ssh <ireland-host>   # host/credentials provided out-of-band by founder
cd /app   # or the Ireland executor working directory
```

## 2. Check running processes

```
ps aux | grep -Ei 'contur|queue|executor|ireland|node|python' | grep -v grep
```

## 3. Check hard-stop flags (per repo docs/source)

```
ls -la *.STOP STOP_* 2>/dev/null || true
env | grep -Ei 'EXECUTOR_|KILL|HALT|DRY' | sed 's/=.*/=<redacted>/'
```

## 4. Check env presence WITHOUT printing secrets

```
for v in SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY EXECUTOR_CANDIDATES_SECRET; do
  if [ -n "${!v}" ]; then echo "$v=PRESENT"; else echo "$v=MISSING"; fi
done
```

## 5. Check latest canonical logs (pulled/synced from PREMVP if shared FS)

```
ls -lt reports/contur3/live_funnel_latest.* 2>/dev/null | head
sed -n '1,40p' reports/contur3/live_funnel_latest.md 2>/dev/null || true
```

## 6. Verify queue pull from PREMVP WITHOUT sending an order

```
# Read-only/dry visibility of the executor queue. Must NOT place orders.
curl -s "$PREMVP_BASE_URL/api/executor/queue?dry=1" \
  -H "x-executor-secret: $EXECUTOR_CANDIDATES_SECRET" | head -c 2000
```

## 7. Start queue monitor in dry / fail-closed mode (only if supported)

```
# Replace with the repo's sanctioned monitor entrypoint that supports --dry-run.
# Example shape only — confirm the real script before running:
# node scripts/executor/queue-monitor.mjs --dry-run --fail-closed
```

## 8. Tail logs

```
tail -f reports/contur3/live_funnel_events.ndjson
```

## 9. Conditions for live approval

- Canonical `live_funnel_latest.md` shows `machine_verdict =
  QUEUE_READY_IRELAND_MANUAL_START_REQUIRED` and `hard_anomaly_count = 0`.
- Queue rows are API-visible with full candidate identity (condition_id,
  token_id, side, stake_usd, order_key, start_time, expires_at, tier/fallback).
- Founder gives explicit live-execution approval for THIS due window.

## 10. Emergency stop

```
# Stop the monitor/executor process (fail-closed). Adjust to the real process name.
pkill -f 'queue-monitor' || true
# Then re-run the canonical log and report state:
# (on PREMVP/Railway) npm run contur3:live-funnel-log
```
