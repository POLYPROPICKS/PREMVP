SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ---------------------------------------------------------------------------
# Payment / Whop v0.1
# ---------------------------------------------------------------------------
PAYMENT_PROVIDER=whop
# Set to true only after SQL schema applied and checkout route is deployed.
CHECKOUT_ENABLED=false
NEXT_PUBLIC_APP_URL=https://polypropicks.com

# WHOP_API_KEY must be a fresh production key.
# Do NOT use any screenshot-exposed proof/test key here.
WHOP_API_KEY=

# Webhook signing secret — copy from Whop Dashboard > Webhooks > your endpoint.
WHOP_WEBHOOK_SECRET=

# Public business identifier (not secret).
WHOP_COMPANY_ID=biz_6LaDCFvRiwJRsa

# Production product IDs per plan.
# WHOP_PRODUCT_ID_7DAY — PolyProPicks 7-Day Premium ($15/week recurring).
WHOP_PRODUCT_ID_7DAY=prod_YAqKFb1sqfcm5
# WHOP_PRODUCT_ID_MONTHLY — PolyProPicks Monthly Pro ($49/month recurring).
WHOP_PRODUCT_ID_MONTHLY=prod_6yFhddOB9v9aA

# Legacy/test product — do NOT use for production checkout.
WHOP_PRODUCT_ID=prod_XLfbDp2YFkaZJ

# Signed session cookie secret — generate a strong random string before enabling.
PPP_SESSION_SECRET=

# ---------------------------------------------------------------------------
# Magic Link email delivery (Resend)
# ---------------------------------------------------------------------------
# Required for /api/auth/magic-link/request to send restore-access emails.
# Sign up at https://resend.com — verify your sending domain before production.
# EMAIL_FROM must match a verified domain in your Resend account, e.g.:
#   noreply@polypropicks.com
RESEND_API_KEY=
EMAIL_FROM=

# Do NOT commit real secret values to version control.
