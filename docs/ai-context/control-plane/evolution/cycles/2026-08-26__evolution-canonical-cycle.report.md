# Daily Evolution Review

Период: 2026-08-26T00:00:00Z — 2026-08-28T00:00:00Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): застряли.
По системе (переиспользуемые возможности): существующая возможность стала прочнее.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- Убран жёсткий потолок в 200 событий у research-scorer — он отбрасывал 581 из 781 подходящих для оценки событий без учёта остатка; теперь прогон ограничен бюджетом по времени (RESEARCH_SCORER_DEFAULT_BUDGET_MS) с явным итоговым статусом для каждого кандидата.
- Убран позиционный потолок limit*3 (=45) у primary pre-scorer — он урезал 587 ликвидных физических событий до 45 входов; заменён на подтверждённую пропускную способность (254) и явный бюджет по времени (wall-clock) для последовательного основного цикла.
- Допуск в деньгах Contract A Planning / Reservation ограничен ровно одной выбранной продакшн-популяцией сигналов (v2-lite-growth-safe); теневые популяции по-прежнему сохраняются и анализируются, но больше не авторитетны для денег.
- dataCoverage больше не накладывает отдельный числовой потолок на отображаемый Signal Score поверх своей роли как взвешенного признака — убран источник схлопывания разных оценок в одну при тонком покрытии данными.
- Модельный гейт B2 перед Reservation (сохранённый Signal Score >= 65, цена входа >= 0.30, исключены eSports) теперь обязателен для решения Contract A Planning до того, как оно станет Reservation.
- Первичное физическое событие с непригодным основным рынком больше не отбрасывается целиком, если у того же события уже есть валидный полноценный рынок-«сосед».
- Старые повторные callback-и Ireland (accepted_open) без поля submitted_size больше не приводят к HTTP 500; requested_shares восстанавливается из stake_usd / submitted_price, когда submitted_size отсутствует.

Какой следующий проверяемый факт в проде стал возможен: Один естественный прод-цикл, прогнанный против исправленных потолков, единой популяции и B2-гейта, который подтвердил бы критерии готовности Phase 1 из CURRENT_STATE.yaml — такой прогон ещё не выполнялся и не наблюдался..

Снятые блокеры:
- Сняты два независимых искусственных числовых потолка (200 у research-scorer, limit*3 у primary pre-scorer), которые молча отбрасывали большинство подходящих кандидатов ещё до продуктовых проверок; заменены на подтверждённые ёмкости с явными бюджетами по времени.
- Снята неоднозначность, какая продакшн-популяция сигналов авторитетна для денег в Contract A Planning/Reservation — теперь во всех трёх точках допуска в деньги действует ровно одна популяция.

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- Формат Evolution Cycle получил версию 1.1 с контрактом телеметрии: счётчики действий Founder, учёт дефектов по причине и повторяемости, отслеживание цепочек «луковичных» дефектов — всё это проверяется отдельным валидатором, а не остаётся текстом ревьюера. (остаётся в репозитории: scripts/control-plane/lib/evolution-cycle.mjs)
- Отдельная библиотека control-plane-reconcile снижает ручное редактирование CURRENT_STATE.yaml после деплоя или merge за счёт автоматической сверки фактического состояния. (остаётся в репозитории: scripts/control-plane/lib/control-plane-reconcile.mjs)
- Automation Roadmap Governor теперь умеет читать реальные сохранённые Evolution-циклы как вход для оценки применимости, а не только синтетические тестовые фикстуры — закрыта часть разрыва между Stage 1 и Stage 2, на который указывала сама политика. (остаётся в репозитории: scripts/control-plane/lib/evolution-governor.mjs)
- Ревьюер Contur gate вызван, расписка сохранена для реального релизного кандидата (31340683) — первое подтверждение, что расписка оформляется на живом кандидате, а не только по спецификации. (остаётся в репозитории: docs/ai-context/control-plane/reviews/release-decap-primary-budget-guard-31340683/CONTUR_GATE_EXACT_SHA_REVIEW_RECEIPT.json)

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- Формат Evolution Cycle получил версию 1.1 с контрактом телеметрии: счётчики действий Founder, учёт дефектов по причине и повторяемости, отслеживание цепочек «луковичных» дефектов — всё это проверяется отдельным валидатором, а не остаётся текстом ревьюера. — Этот цикл сам сделан версией 1.1 и проверяется по этому контракту при записи; набор тестов Evolution вырос на 89 строк в том же коммите.
- Отдельная библиотека control-plane-reconcile снижает ручное редактирование CURRENT_STATE.yaml после деплоя или merge за счёт автоматической сверки фактического состояния. — tests/control-plane/controlPlaneReconcileBaseline.test.mjs расширен на +23 строки в том же коммите; validate-control-plane.mjs и reconcile-control-plane.mjs переведены на новую библиотеку и слиты в PR#185.
- Automation Roadmap Governor теперь умеет читать реальные сохранённые Evolution-циклы как вход для оценки применимости, а не только синтетические тестовые фикстуры — закрыта часть разрыва между Stage 1 и Stage 2, на который указывала сама политика. — tests/control-plane/evolutionGovernor.test.mjs расширен на 218 строк в том же коммите, что добавил чтение сохранённых циклов; слито в PR#186.
- Ревьюер Contur gate вызван, расписка сохранена для реального релизного кандидата (31340683) — первое подтверждение, что расписка оформляется на живом кандидате, а не только по спецификации. — Ссылается на проверенную цепочку коммитов и 13 из 13 тестов на этом SHA; PR#196 слил расписку в origin/main.

## Что блокирует следующий шаг

- Изменили ли эти семь слитых исправлений реальный результат Planning/Reservation в проде — не наблюдалось. CURRENT_STATE.yaml по-прежнему фиксирует проверку готовности Phase 1 как WAIT (временная ошибка кэша схемы Supabase), без изменений с начала периода.
- За этот период нет доказательств исполнений, комиссий или расчёта. Сверенный PnL не заявляется и не выводится из исправлений на уровне кода.
- Влияние любого из одиннадцати слитых PR на выручку или бизнес независимо не подтверждено — за пределами самого merge и его тестов.
- Срабатывал ли на практике бюджет по времени из коммита 31340683 (то есть пропускались ли кандидаты) — не наблюдалось; телеметрия продакшена за этот период не читалась.
- Корректность чтения Governor-ом сохранённых циклов при более чем одном каноническом цикле не проверена — до этого запуска существовал только один канонический цикл (2026-08-25).
- CI-гейт для проверки расписок ревьюера по-прежнему только PLANNED, не доказан, согласно AGENT_REGISTRY.yaml — расписка Contur за этот период оформлена и проверена вручную, а не машиной, это тот же открытый блокер BLK-002 из CURRENT_STATE.yaml.

## Варианты автоматизации

- Общий, дописываемый только в конец журнал действий оператора, куда оба исполнителя пишут в момент сообщения; evolution-collect.mjs читает его напрямую вместо вручную собранного бандла. — система позже.
  Проблема: У этого цикла нет прямой видимости ни одного действия Founder/Codex/CloudCode за весь период 2026-08-26 — 2026-08-28 (хуже, чем частичное покрытие цикла 2026-08-25 — у этого разбора вообще нет стенограммы сессии за этот период), поэтому полнота сбора помечена как «неизвестно», а не «частично».
  Что останется в репозитории: docs/ai-context/control-plane/evolution/OPERATOR_ACTION_POLICY.yaml
  Когда остановиться: Остановиться, если потребует менять то, как исполнители запускают сессии, или не получится остаться без зависимостей и офлайн
- Скрипт только для чтения, который перечисляет merge-коммиты в origin/main за период, выдаёт их как список кандидатов в confirmed_changes и готовит черновик записи EVIDENCE_LEDGER.md для проверки архитектором/фаундером (без автоматического коммита). — система позже.
  Проблема: Список подтверждённых изменений для этого цикла снова собран вручную чтением 'git log --merges' за период. Кроме того, последняя запись EVIDENCE_LEDGER.md (EV-0020) датирована 2026-08-13 — более 20 merge в origin/main с тех пор, включая четыре важных для Axis B merge в этом периоде (#184, #185, #186, #196), не имеют соответствующей записи в журнале.
  Что останется в репозитории: scripts/control-plane/evolution-collect.mjs
  Когда остановиться: Остановиться, если вывод скрипта когда-либо используют для вывода бизнес-эффекта, а не только списка merge, или если его подключат к автозаписи EVIDENCE_LEDGER.md без проверки
- Скрипт-lint только для чтения, который помечает диф, снимающий фиксированную числовую константу ёмкости (по шаблону имени *_CAP, *_LIMIT), если тот же диф не добавляет и не имеет рядом парную константу *_BUDGET_MS. — система позже.
  Проблема: Снятие искусственного позиционного потолка primary pre-scorer (коммит 7ebad82) обошлось без бюджета по времени; страховку пришлось добавлять отдельным коммитом (31340683) в тот же день, после того как риск был описан в самом тексте коммита. При снятии потолка research-scorer (b2de038) бюджет по времени добавили сразу в том же коммите — практика непоследовательна.
  Что останется в репозитории: scripts/control-plane (новый прототип скрипта, ещё не создан)
  Когда остановиться: Остановиться, если ложных срабатываний окажется больше, чем верных находок, на двух оценённых PR
- Расширить существующий валидатор конвертов завершения (или добавить отдельный узкий скрипт), чтобы он проверял отдельный JSON-файл расписки ревьюера под docs/ai-context/control-plane/reviews/** по соответствующей подсхеме — как pre-commit или CI-проверку. — система позже.
  Проблема: Расписка Contur gate для кандидата 31340683 оформлена вручную и закоммичена напрямую; ничто не показывает, что перед этим прошла автоматическая проверка схемы. CI-гейт для этого по-прежнему только PLANNED, тот же открытый блокер BLK-002.
  Что останется в репозитории: scripts/control-plane/validate-completion-envelope.mjs
  Когда остановиться: Остановиться, если формы расписок Weather и Contur разойдутся настолько, что одна проверка не сможет покрыть обе, не ослабив ни один из гейтов
- Небольшая библиотека фикстур с известными старыми формами callback (без submitted_size и другими будущими вариантами), против которой прогоняются изменения buildEconomicTelemetry/buildExecutionReconciliation как часть их существующих тестов. — сначала продукт.
  Проблема: Дефект со старыми callback-ами Horbury (accepted_open без submitted_size) показывает, что обязательное поле, добавленное в buildEconomicTelemetry/buildExecutionReconciliation, не было проверено против всех старых форм полезной нагрузки до того, как повторные попытки начали падать с HTTP 500.
  Что останется в репозитории: tests/contur3/economicTelemetryLegacyAcceptedOpenCallback.test.ts
  Когда остановиться: Остановиться, если старые формы полезной нагрузки окажутся слишком разнообразными для ограниченного набора фикстур

## Две практики Founder

- Читать точный блок готовности рантайма в CURRENT_STATE.yaml перед тем, как засчитывать любое число слитых PR как движение бизнеса — вместо того чтобы судить о прогрессе по объёму merge.
  Зачем сейчас: В этом периоде было одиннадцать слитых PR, и по объёму это выглядело как сильный прогресс; но сам блок готовности рантайма не изменился по сути (всё ещё WAIT) ни до, ни после периода — а именно этот факт определяет вердикт по Axis A.
  Как ложится на проект: Та же проверка применима к каждому будущему Daily Evolution Review и к любому статусу для Founder: активность на уровне кода и доказательство рантайма/бизнеса — разные вопросы, и ответ на второй даёт только сам блок готовности рантайма в CURRENT_STATE.yaml.
  Что останется в репозитории: docs/ai-context/control-plane/CURRENT_STATE.yaml
- Распознавать скрытый дефект следующего слоя по тексту коммита, который сам описывает свою причинно-следственную связь, не дожидаясь отдельного доказательства из рантайма.
  Зачем сейчас: Коммит 31340683 прямо говорит, что снятие потолка в 7ebad82 и обнажило риск по времени, который он же чинит — этой фразы достаточно, чтобы классифицировать дефект как «скрытый следующий слой» с реальной цепочкой, не дожидаясь отдельного прод-инцидента для доказательства причинности.
  Как ложится на проект: То же чтение применимо к любому будущему PR, чей текст коммита рассказывает «снятие X обнажило Y» — это бесплатный и надёжный сигнал «луковичной» цепочки, который стоит искать в текстах коммитов перед тем, как считать дефект независимым и новым.
  Что останется в репозитории: tests/feed/primaryLoopWallClockGuard.test.ts

Сравнение: P1 защищает корректность Axis A, не давая объёму merge подменить доказательство рантайма или бизнеса. P2 ускоряет верную классификацию цепочек дефектов, используя причинный текст самих коммитов вместо ожидания свежих доказательств из рантайма. P1 защищает от самой соблазнительной ложной победы во всём этом разборе; P2 удешевляет раздел учёта дефектов.
Рекомендуемый порядок: сначала P1, затем P2.

## Следующие эксперименты

- Статическая проверка, помечающая диф, снимающий фиксированную числовую константу ёмкости без парной константы бюджета по времени в том же дифе, поймала бы паттерн DCH-PRIMARY-SCORER-CAP-TO-BUDGET-20260827 до того, как потребовался отдельный коммит.
  Границы: Один период, только набросок скрипта только для чтения, прогнанный вручную на двух существующих коммитах (7ebad82, 31340683) как первый тестовый случай — без подключения к CI.
  Что останется: scripts/control-plane/check-capacity-guard-pairing.mjs (предложен, ещё не создан)
  Считаем удачей: Набросок верно помечает историческое состояние 7ebad82 без страховки и не помечает объединённое состояние 7ebad82+31340683 при ручном прогоне.
  Останавливаемся, если: Остановиться, если ложных срабатываний окажется больше, чем верных находок, на небольшой выборке прошлых PR.
- Скрипт только для чтения, который перечисляет merge-коммиты в origin/main за период и готовит черновики confirmed_changes и записи EVIDENCE_LEDGER.md, закрыл бы разрыв, который уже сохраняется нерешённым два цикла подряд (2026-08-25 и этот).
  Границы: Один период, только прототип скрипта: перечислить merge между двумя метками времени и подготовить черновики записей журнала для проверки человеком/архитектором; без автозаписи в EVIDENCE_LEDGER.md.
  Что останется: scripts/control-plane/evolution-collect.mjs
  Считаем удачей: В следующем цикле список confirmed_changes И хотя бы одна черновая запись EVIDENCE_LEDGER.md сгенерированы скриптом и только проверены, а не напечатаны вручную.
  Останавливаемся, если: Остановиться, если вывод скрипта когда-либо автоматически закоммитят в EVIDENCE_LEDGER.md без проверки человеком или архитектором.
- Расширение существующего валидатора конвертов завершения для проверки отдельного JSON-файла расписки ревьюера позволило бы машинно проверять расписку 31340683 (и будущие) до коммита, сужая открытый блокер BLK-002.
  Границы: Один период, только прототип валидатора: прогнать существующую подсхему расписки ревьюера против реальной расписки 31340683 и одной намеренно сломанной тестовой; без подключения к CI.
  Что останется: scripts/control-plane/validate-completion-envelope.mjs
  Считаем удачей: Прототип верно принимает реальную расписку 31340683 и верно отклоняет сломанную тестовую.
  Останавливаемся, если: Остановиться, если формы расписок Weather и Contur разойдутся настолько, что одна проверка не сможет покрыть обе, не ослабив ни один из гейтов.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: неизвестно
- стоимость одного проверенного результата: неизвестно
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: 0
- создано переиспользуемых артефактов: 10
- ручных сообщений в CloudCode: 0
- ручных сообщений в Codex: 0
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 0 (полнота сбора — неизвестен).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 1 — ежедневный разбор.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Сохранить этот цикл в origin/main через уже привязанный к этой сессии цикл branch/PR, затем остановиться — без опроса состояния, без активации расписания, без реализации автоматизации и без принятия изменений в CURRENT_STATE. Следующий разбор должен покрыть период с 2026-08-28T00:00:00Z и далее.
