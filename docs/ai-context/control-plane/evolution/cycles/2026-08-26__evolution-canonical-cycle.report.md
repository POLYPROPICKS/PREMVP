# Daily Evolution Review

Период: 2026-08-26T00:00:00Z — 2026-08-28T00:00:00Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): недостаточно доказательств.
По системе (переиспользуемые возможности): существующая возможность стала прочнее.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- Из воронки событий убрали два потолка: лимит 200 у research-скорера (PR#188) и обрез 45 у primary-scorer (PR#197); там же добавили ограничение по времени (wall-clock guard), которое понадобилось из-за снятия второго потолка. Раньше один прогон терял 581 из 781 и 587 подходящих событий на этих потолках.
- Contract A теперь допускает к деньгам (Planning/Reservation) только одну каноническую популяцию сигналов (v2-lite-growth-safe); теневые research/strategic популяции больше не могут стать денежным решением (PR#191).
- Политика B2 (score от 65, цена входа от 0.30, без eSports) закреплена как обязательный шаг перед Reservation в Contract A Planning, шаг 3 из 5 (PR#194).
- Исправлена ошибка легаси-колбэков Ireland: старые записи без submitted_size роняли повтор отправки экономической телеметрии в HTTP 500; теперь requested_shares восстанавливается из stake_usd и цены (PR#187).

Какой следующий проверяемый факт в проде стал возможен: Можно за один read-only прогон production_observation.v1 проверить, действительно ли B2 и единая каноническая популяция меняют, какие кандидаты становятся денежными Reservation — в этом цикле такой прогон не делали..

Снятые блокеры:
- Ошибка HTTP 500 в легаси-колбэках Ireland, блокировавшая повтор записи экономической телеметрии, устранена на уровне кода.

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- Схема EVOLUTION_CYCLE выросла до версии 1.1: добавлены срок отсечки доказательств и подробная операционная телеметрия с дефектами и цепочками причин. (остаётся в репозитории: docs/ai-context/control-plane/evolution/schemas/EVOLUTION_CYCLE.schema.json)
- Automation Roadmap Governor (Stage 2) теперь читает канонические сохранённые Evolution-циклы напрямую из cycles/, а не собранный вручную вход. (остаётся в репозитории: scripts/control-plane/lib/evolution-governor.mjs)
- Добавлена не зависящая от внешних пакетов библиотека сверки состояния control-plane с живым Git — считает факт-делту для CURRENT_STATE.yaml, но в этом периоде не запускалась для реального обновления файла. (остаётся в репозитории: scripts/control-plane/lib/control-plane-reconcile.mjs)

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- Схема EVOLUTION_CYCLE выросла до версии 1.1: добавлены срок отсечки доказательств и подробная операционная телеметрия с дефектами и цепочками причин. — Первое живое использование схемы 1.1; 57 из 57 тестов evolutionDailyReview.test.mjs прошли в этом цикле, включая 5 новых тестов на телеметрию 1.1.
- Automation Roadmap Governor (Stage 2) теперь читает канонические сохранённые Evolution-циклы напрямую из cycles/, а не собранный вручную вход. — 52 из 52 тестов evolutionGovernor.test.mjs перезапущены в этом цикле и прошли.
- Добавлена не зависящая от внешних пакетов библиотека сверки состояния control-plane с живым Git — считает факт-делту для CURRENT_STATE.yaml, но в этом периоде не запускалась для реального обновления файла. — Слито вместе с обновлёнными тестами (controlPlaneReconcileBaseline, premvpReleasePipeline, validateControlPlane); в этом цикле повторно не перезапускались.

## Что блокирует следующий шаг

- В этом цикле не было ни одного read-only наблюдения за продом, поэтому ни один из пунктов выше не подтверждён вживую — только на уровне кода и тестов.
- Исполнения, комиссии и расчёт не собирались и не заявляются; сверенный PnL за этот цикл не доказан вообще.
- Не подтверждено, что фикс легаси-колбэков реально помог конкретному повтору очереди 3e7ed426 у Ireland — проверена только сама логика восстановления и её условия.
- CURRENT_STATE.yaml и EVIDENCE_LEDGER.md всё ещё датированы 13 и 07 августа — см. дефект устаревания состояния ниже; значит roadmap_phase и открытые блокеры, на которые опирается этот разбор, не обновлялись под 11 PR этого периода.
- Новая библиотека сверки состояния (PR#185) ни разу не запускалась на самом репозитории, чтобы реально обновить CURRENT_STATE.yaml и EVIDENCE_LEDGER.md — способность появилась, но разрыв, который она должна закрывать, ещё открыт.
- Тесты controlPlaneReconcileBaseline, premvpReleasePipeline и validateControlPlane не перезапускались в этом цикле; их прохождение взято из момента слияния PR, а не проверено заново здесь.
- Сохранение именно этого цикла сломало 3 теста в evolutionGovernor.test.mjs, которые жёстко зашивают содержимое живой папки cycles/ как фикстуру (см. дефект DEF-2026-08-28-GOVERNOR-TEST-HARDCODED-CYCLE-COUNT и H7); control-plane:check не может быть чисто зелёным после этого сохранения, пока тесты не переписаны отдельной задачей.

## Варианты автоматизации

- Подключить scripts/control-plane/reconcile-control-plane.mjs либо как CI-гейт (падает, если origin_main_sha в CURRENT_STATE отстаёт от живого origin/main больше чем на N слияний), либо как отдельный Routine, который читает состояние в read-only режиме и открывает PR с предложенной делтой на подтверждение Founder. — делать сейчас.
  Проблема: CURRENT_STATE.yaml и EVIDENCE_LEDGER.md всё ещё от 13 и 07 августа, хотя за 2 дня этого периода слилось 11 PR — включая PR#185, который построил как раз автоматизацию для дешёвой сверки, но она ни разу не запускалась.
  Что останется в репозитории: scripts/control-plane/reconcile-control-plane.mjs
  Когда остановиться: Остановиться, если скрипт сверки потребует доступ к продовому runtime или базе данных, либо если Founder отклонит предложенную делту.
- Общий помощник-проверка для любой границы воронки: сколько кандидатов вошло должно равняться сумме терминальных причин выхода — обобщение уже реализованного вручную паттерна (primaryCandidatesEntered/primaryTerminalReasonCounts и attempted=selected+scoredRejected+notScored). — система позже.
  Проблема: Два независимых искусственных потолка в одной воронке событий нашли и починили по отдельности за 24 часа (PR#188, затем PR#197); снятие второго потолка тут же обнажило третий скрытый дефект (wall-clock), который тоже пришлось чинить в том же PR.
  Что останется в репозитории: tests/feed/lib/populationConservation.ts (новый)
  Когда остановиться: Остановиться, если у стадий воронки окажутся несовместимые формы терминальных причин, которые нельзя свести к одному контракту проверки.
- Статическая проверка (lint-правило или скрипт control-plane), которая помечает голые числовые потолки в коде скоринга и допуска в воронку, если они не оформлены именованной константой с обоснованием — по образцу уже введённого в этом периоде PRIMARY_SCORER_PROVEN_CAPACITY. — система позже.
  Проблема: Три отдельно найденных дефекта этого периода (потолок 200 у research-скорера, обрез 45 у primary pre-scorer, потолок Signal Score от dataCoverage) — один и тот же паттерн: недокументированное число-потолок без именованной обоснованной константы и без теста на то, что оно осознанное.
  Что останется в репозитории: новое eslint-правило или скрипт control-plane (пока не создано)
  Когда остановиться: Остановиться, если правило будет слишком часто ложно срабатывать на легитимные именованные числа (размеры страниц, таймауты).
- То же предложение, что и в цикле от 25 августа: общий, дозаписываемый лог операторских действий, куда оба исполнителя пишут в момент отправки сообщения, и который evolution-collect.mjs читает напрямую, а не собирает по веткам вручную. — делать сейчас.
  Проблема: В этом цикле удалось установить только нижнюю границу — 11 миссий Founder по отдельным git-веткам; ни одной первичной записи операторского действия нет, тот же разрыв, что и H1 в цикле от 25 августа, только теперь на порядок больше миссий за те же 2 дня.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/OPERATOR_ACTION_POLICY.yaml
  Когда остановиться: Остановиться, если это потребует менять то, как исполнители стартуют сессии, или лог перестанет быть офлайн и без внешних зависимостей.
- Ограниченный read-only аудит требуемых полей buildEconomicTelemetry/buildExecutionReconciliation против реального диапазона исторических форм колбэков — чтобы найти похожие пробелы до того, как они уронят живой повтор. — сначала продукт.
  Проблема: Фикс легаси-колбэка Horbury (PR#187) — точечный патч под один конкретный элемент очереди из-за одной необработанной старой формы данных (нет submitted_size); в пути экономической телеметрии могут быть и другие непроверенные легаси-формы.
  Что останется в репозитории: новый отчёт-аудит в docs/ai-context/control-plane/reviews/ (пока не создан)
  Когда остановиться: Остановиться, если аудит потребует доступ на запись к продовым данным колбэков, а не только SELECT.
- Один ограниченный read-only прогон уже зарегистрированной команды production_observation.v1, нацеленный именно на решения Contract A Planning, прошедшие через гейт B2. — делать сейчас.
  Проблема: Привязка политики B2 к Contract A Planning (PR#194) подтверждена только на уровне кода и тестов (13 тестов) — ни одного наблюдения за тем, как она ведёт себя на живом планировании, ещё нет.
  Что останется в репозитории: reports/observation/<observation_id>.json (уже существующий контракт вывода команды)
  Когда остановиться: Остановиться, если наблюдение дважды подряд вернёт статус ожидания инфраструктуры вместо реального результата по B2.
- Rewrite the three affected tests (discoverCanonicalEvolutionCycles/prepareGovernorEvidence/CLI assertions around line 186, 273 and 301 of evolutionGovernor.test.mjs) to read the real cycles/ directory count dynamically, or to run against an isolated temporary fixture directory instead of the live persisted history. — делать сейчас.
  Проблема: Persisting this very cycle (the normal, intended action of this Routine) broke 3 pre-existing tests in tests/control-plane/evolutionGovernor.test.mjs, which hardcode the live cycles/ directory content as an expected fixture: exactly one cycle, with the specific id 2026-08-25__evolution-canonical-cycle.
  Что останется в репозитории: tests/control-plane/evolutionGovernor.test.mjs
  Когда остановиться: Stop if decoupling these tests from the live directory would hide a real Governor discovery regression that the current hardcoded assertion was actually catching.

## Две практики Founder

- Не считать смёрженную и протестированную работу продвижением к запуску, пока для неё нет наблюдения за живым продом или бизнес-результатом.
  Зачем сейчас: Только за этот период смержили четыре готовых на уровне кода изменения (B2, единая популяция, два снятых потолка) — и ни разу не собрали наблюдение за продом ни для одного из них.
  Как ложится на проект: Перед следующим планом Архитектора запустить по одному read-only прогону production_observation.v1 на каждое ещё не проверенное изменение этого периода, начиная с B2, и только потом считать это продвижением к запуску.
  Что останется в репозитории: scripts/control-plane/production-observation.mjs
- Диагностировать обрезание воронки по точным цифрам из телеметрии прода, а не по ощущению, что событий маловато.
  Зачем сейчас: Именно эта дисциплина нашла и починила все три необоснованных потолка в этом периоде; на расплывчатой формулировке скорее всего остановились бы на первом же найденном потолке.
  Как ложится на проект: Применить ту же дисциплину точных цифр к следующей непроверенной границе воронки — допуску из Contract A Planning в Reservation — прежде чем считать её чистой.
  Что останется в репозитории: lib/feed/buildLandingCards.ts

Сравнение: P1 защищает от того, чтобы смёрженная работа записывалась как готовая раньше времени; P2 — уже сработавший в этом периоде метод поиска дефектов. P1 про то, что записывается в отчёт, P2 — про то, что находится в коде.
Рекомендуемый порядок: сначала P1, затем P2.

## Следующие эксперименты

- Один общий помощник-проверка сохранения количества кандидатов на границе воронки поймает следующий дефект-обрезание без отдельного патча каждый раз.
  Границы: В рамках одного периода — только прототип: описать контракт проверки и применить его к уже починенной стадии primary loop; новую стадию воронки не трогать.
  Что останется: tests/feed/lib/populationConservation.ts (новый)
  Считаем удачей: В следующем цикле фикс на другой стадии воронки переиспользует этот помощник без нового отдельного кода проверки.
  Останавливаемся, если: Остановиться, если у стадий воронки окажутся несовместимые формы терминальных причин.
- reconcile-control-plane.mjs, смёрженный именно для дешёвой сверки CURRENT_STATE.yaml, может закрыть дефект устаревания состояния из этого же цикла без единой строчки нового кода.
  Границы: Один read-only прогон уже существующей библиотеки сверки против живого origin/main, только предложенная делта; этот разбор сам делту не принимает и не пишет.
  Что останется: scripts/control-plane/lib/control-plane-reconcile.mjs (уже смёржен; эксперимент проверяет использование, не существование)
  Считаем удачей: Прогон без ошибок покажет ровно ту делту, которую дают 11 PR этого периода, и отдельная авторизованная задача её применит.
  Останавливаемся, если: Остановиться, если скрипт упадёт с ошибкой или потребует доступ к продовому runtime или базе данных.
- production_observation.v1, уже зарегистрированная и read-only, может независимо подтвердить, как политика B2 ведёт себя в проде, без нового кода.
  Границы: Один read-only прогон наблюдения, нацеленный на решения Contract A Planning, прошедшие через B2, в следующем естественном цикле планирования.
  Что останется: reports/observation/<observation_id>.json (уже существующий контракт вывода команды)
  Считаем удачей: Наблюдение вернёт подтверждение продового эффекта для решений, прошедших B2.
  Останавливаемся, если: Остановиться, если наблюдение дважды подряд вернёт статус ожидания инфраструктуры вместо реального результата.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: неизвестно
- стоимость одного проверенного результата: неизвестно
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: 3
- создано переиспользуемых артефактов: 3
- ручных сообщений в CloudCode: 8
- ручных сообщений в Codex: 3
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 11 (полнота сбора — частичный).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 2 — управление автоматизацией.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Сохранить цикл в origin/main. Ожидаемо: control-plane:check покажет 3 красных теста в evolutionGovernor.test.mjs (см. H7) — известное следствие сохранения второго цикла, чинит отдельная задача, не этот Routine. Порог Governor ещё не набран, Stage 2 не запускается. Отдельно прогнать reconcile-control-plane.mjs в read-only режиме и увидеть делту CURRENT_STATE.yaml.
