# Daily Evolution Review

Период: 2026-08-07T10:47:52Z — 2026-08-08T13:53:49Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): недостаточно доказательств.
По системе (переиспользуемые возможности): появилась новая переиспользуемая возможность.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- Завершена работа над идентификацией продюсеров/провайдеров на «теневых» строках в фид-пайплайне (lib/feed/discoverSportsMarkets.ts, lib/feed/polymarketClient.ts, lib/feed/cacheGeneratedSignals.ts), добавлен отдельный регрессионный набор тестов на сохранение идентичности.
- Внесено точечное исправление в поиск точной пары сигналов планировщика ребалансировки — в том же модуле eventExecutionQueue.ts, который отвечает за гарантию неизменности очереди в C1.
- Добавлен доступный только для чтения репортер forward-funnel для Contur3, встроенный в код решений Planning/Contract-A — это следующий шаг дорожной карты STEP_3_FORWARD_FUNNEL.
- Построена и зарегистрирована (PROVEN_PRESENT, покрыта тестами) возобновляемая команда наблюдения за производственным эффектом (premvp.command.production_observation.v1), способная доказать, прошёл ли естественный ночной прогон резерваций через C1 — но в этом периоде она ещё не запускалась против текущего деплоя.

Какой следующий проверяемый факт в проде стал возможен: Запуск новой команды наблюдения за производством на текущей задеплоенной версии теперь может дать первый доказанный ответ на вопрос, прошёл ли естественный ночной прогон резерваций проверку C1 после последнего подтверждённого деплоя — раньше в этом периоде такой проверки не существовало.

Появившиеся блокеры:
- CURRENT_STATE.yaml теперь требует обновления (STATE_REFRESH_REQUIRED): его базовая версия a942a0d8 является предком текущего origin/main (e06b6653), но 23 изменённых пути выходят за рамки разрешённого списка из трёх файлов для «состояния без изменений продукта», включая продуктовый код в lib/executor и lib/feed.

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- Добавлена возобновляемая команда наблюдения за производственным эффектом (только чтение) с именованной машиной состояний восстановления и идемпотентным возобновлением по observation_id, отделяющая сбои инфраструктуры от настоящих ошибок. (остаётся в репозитории: scripts/control-plane/production-observation.mjs)
- Зарегистрированный релизный пайплайн PREMVP получил реальную исполняемую функцию оркестрации (тесты, ревью-гейт, идемпотентные PR/merge, опрос деплоя) и теперь отмечен как ENABLED вместо прежнего экспериментального состояния-заглушки. (остаётся в репозитории: scripts/control-plane/lib/premvp-release-pipeline.mjs)
- Добавлен доступный только для чтения forward-funnel репортер для Contur3 как самостоятельный скрипт, независимый от боевого пути принятия решений исполнителя. (остаётся в репозитории: scripts/contur3/forward-funnel-report.ts)

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- Построена и зарегистрирована (PROVEN_PRESENT, покрыта тестами) возобновляемая команда наблюдения за производственным эффектом (premvp.command.production_observation.v1), способная доказать, прошёл ли естественный ночной прогон резерваций через C1 — но в этом периоде она ещё не запускалась против текущего деплоя.
- Добавлена возобновляемая команда наблюдения за производственным эффектом (только чтение) с именованной машиной состояний восстановления и идемпотентным возобновлением по observation_id, отделяющая сбои инфраструктуры от настоящих ошибок. — Зарегистрирована как PROVEN_PRESENT и покрыта собственным набором тестов; полный прогон control-plane тестов прошёл 193/193 в этой сессии.
- Зарегистрированный релизный пайплайн PREMVP получил реальную исполняемую функцию оркестрации (тесты, ревью-гейт, идемпотентные PR/merge, опрос деплоя) и теперь отмечен как ENABLED вместо прежнего экспериментального состояния-заглушки. — Покрыта набором тестов пайплайна с добавленными тестами на идемпотентное возобновление; запись в реестре отмечена как PROVEN_PRESENT.
- Добавлен доступный только для чтения forward-funnel репортер для Contur3 как самостоятельный скрипт, независимый от боевого пути принятия решений исполнителя. — Покрыт собственным тестовым файлом, проверен вместе со связанными регрессионными тестами в том же успешном прогоне control-plane.

## Что блокирует следующий шаг

- CURRENT_STATE.yaml теперь требует обновления (STATE_REFRESH_REQUIRED): его базовая версия a942a0d8 является предком текущего origin/main (e06b6653), но 23 изменённых пути выходят за рамки разрешённого списка из трёх файлов для «состояния без изменений продукта», включая продуктовый код в lib/executor и lib/feed.
- Задеплоены ли в продакшн изменения из PR#112–#120 (исправления идентификации продюсера, ограничение поиска в очереди, forward-funnel репортер, включение release-пайплайна, команда наблюдения) — проверки build-info или деплоя за этот период нет.
- Изменилось ли что-то по блокеру BLK-001 (нет доказанного естественного ночного прогона после деплоя) — новая команда наблюдения в этом разборе не запускалась.
- Любой сверенный PnL, исполнения, комиссии или расчёт по сделкам — таких данных за период нет вообще.
- Снижают ли исправления идентификации продюсера и поиска в очереди реальные ошибки в продакшне — подтверждены только модульные/регрессионные тесты, не поведение в проде.
- Повторится ли обнаруженное в этом разборе отставание состояния (CURRENT_STATE.yaml отстаёт на 9 слитых PR) — автоматической проверки свежести пока нет.
- Использовались ли три добавленные за период возможности на реальных продакшн-данных, а не только в тестах с заглушками — планка «след использования или проверки» пока выполнена на уровне тестов и реестра, а не боевого запуска.
- Правило *.json в .gitignore (строка 55) незаметно блокирует любой новый JSON-файл, включая будущие циклы Evolution, входные пакеты и предложения по дорожной карте, от попадания в Git без ручного принудительного добавления — это обнаружилось только когда собственные файлы этого цикла не появились в git status.

## Варианты автоматизации

- Скрипт только для чтения, который выполняет задокументированную проверку предка и allowlist-диффа и выдаёт STATE_FRESH / STATE_REFRESH_REQUIRED / STATE_STALE. — делать сейчас.
  Проблема: Собственное правило stale_when в CURRENT_STATE.yaml уже описывает именно эту ситуацию — база является предком origin/main, но изменённые пути выходят за allowlist — однако никто это не проверяет автоматически; в этой сессии потребовалось пять git-команд и восемь файлов, чтобы подтвердить это вручную.
  Что останется в репозитории: scripts/control-plane/check-state-freshness.mjs (новый, ещё не построен)
  Когда остановиться: При ложных срабатываниях на легитимных bootstrap-изменениях состояния — остановиться и расширить тестовые фикстуры allowlist, а не отключать проверку.
- Ограниченный генератор, который по диапазону merge-SHA готовит черновик записи EVIDENCE_LEDGER.md (только факты: номер PR, SHA, изменённые файлы, число тестов) для проверки архитектором — сам ничего не пишет и не принимает. — система позже.
  Проблема: У PR#112–PR#120 — девяти слитых PR — нет ни одной соответствующей записи в EVIDENCE_LEDGER.md, хотя журнал считается основным источником исторических доказательств control plane.
  Что останется в репозитории: scripts/control-plane/draft-evidence-ledger-entry.mjs (новый, ещё не построен)
  Когда остановиться: Если черновики требуют больше правок, чем написание с нуля — отказаться от автоматизации.
- Режим --since-last-cycle у evolution-collect.mjs, который при отсутствии явного пакета сам выводит period_start/period_end и confirmed_changes от последнего сохранённого цикла (или базы CURRENT_STATE) до origin/main, оставляя всю оценку по осям ревьюеру. — система позже.
  Проблема: Этому разбору пришлось вручную восстанавливать свой период (базовый SHA, происхождение, confirmed_changes) примерно пятнадцатью ручными шагами, потому что для 2026-08-07 — 2026-08-08 ещё не было ни входного пакета, ни completion envelope.
  Что останется в репозитории: расширение scripts/control-plane/evolution-collect.mjs плюс тесты
  Когда остановиться: Если выведенный диапазон когда-либо незаметно пропустит merge (пограничный случай squash против merge-коммитов) — остановиться и требовать явные границы от ревьюера.
- Определить и задокументировать один минимальный способ фиксации событий (например, ежедневный журнал, который ведёт Founder или который экспортируется из сессии, с меткой mission_id), который сможет прочитать будущий входной пакет. — сначала продукт.
  Проблема: В этом разборе operator_action_events нечем было заполнить — нигде в Git не хранится журнал ручных сообщений Founder, поэтому поле полноты сбора осталось «неизвестно», и вся группа метрик по действиям оператора непригодна для использования не только в этом, а в любом цикле.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/OPERATOR_ACTION_CAPTURE_SOURCE.md (new, not yet decided)
  Когда остановиться: Если выбранный способ отнимает у Founder больше нескольких секунд на действие — остановиться и искать автоматический экспорт.
- Подтвердить (уже действует благодаря этому запуску) привязку ежедневной рутины из SCHEDULE_MANIFEST.yaml к реальной планировщик-поверхности, управляемой Founder. — делать сейчас.
  Проблема: AUTOMATION_ROADMAP.yaml отмечает Stage 1 как «реализовано, но ещё не опробовано» — возможность Daily Evolution Review существовала, полностью протестированная, с 2026-08-07, но папка cycles/ была пустой до этого запуска — целый день готовая возможность простаивала.
  Что останется в репозитории: JSON и отчёт этого цикла в docs/ai-context/control-plane/evolution/cycles/.
  Когда остановиться: Если ежедневный запуск этой рутины начнёт дублировать или конфликтовать с ручным запуском Founder — остановить один из двух.
- Не новая автоматизация — плановый или инициированный Founder запуск уже построенной команды против текущего задеплоенного SHA после следующего естественного ночного окна резерваций, с записью вердикта в EVIDENCE_LEDGER.md. — делать сейчас.
  Проблема: Команда premvp.command.production_observation.v1 — построенная и зарегистрированная как PROVEN_PRESENT именно в этом периоде, чтобы ответить на блокер BLK-001 (нет доказанного естественного ночного прогона после деплоя) — ещё не запускалась против текущего деплоя; ни одна запись в EVIDENCE_LEDGER не ссылается ни на один observation_id или вердикт.
  Что останется в репозитории: итоговый reports/observation/<observation_id>.json (в .gitignore) плюс новая отслеживаемая запись в EVIDENCE_LEDGER.md со ссылкой на него.
  Когда остановиться: При повторяющемся WAIT_INFRASTRUCTURE_RECOVERY — не повторять запуск чаще, чем позволяет собственный интервал возобновления команды, эскалировать Founder.
- Добавить явные исключения в .gitignore для docs/ai-context/control-plane/evolution/{cycles,input-bundles,corrections,roadmap-proposals}/*.json, либо сузить существующее правило *.json так, чтобы оно не затрагивало docs/ai-context. — делать сейчас.
  Проблема: Собственные JSON-файлы этого разбора (цикл и входной пакет) не появились в git status после записи, потому что правило .gitignore (строка 55, *.json) блокирует все новые JSON-файлы во всём репозитории; сделать их отслеживаемыми удалось только через git add -f.
  Что останется в репозитории: .gitignore
  Когда остановиться: Если расширение исключения случайно откроет путь, игнорируемый по реальной причине (например, локальные секреты или сгенерированный кэш) — сразу сузить исключение.

## Две практики Founder

- Проверка свежести состояния перед планированием — не доверять CURRENT_STATE.yaml, пока свежесть не подтверждена по задокументированному правилу «предок плюс allowlist».
  Зачем сейчас: Этот разбор обнаружил, что собственный снимок control plane отстал от живого origin/main на девять слитых PR — именно тот сбой, который правило stale_when должно ловить, но никто его не запускал.
  Как ложится на проект: Прежде чем принимать план архитектора или заявление исполнителя как основанные на «текущем состоянии», выполнить двухкомандную проверку из этого цикла: убедиться, что записанная база — предок живого origin/main, затем сравнить изменённые пути со списком из трёх разрешённых файлов.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/cycles/2026-08-08__daily-evolution-review.json — фиксирует точные команды и их результат как повторяемую процедуру.
- Различать «зарегистрировано и протестировано» от «признано и прожито» — проверять, что статус PROVEN_PRESENT в AGENT_REGISTRY.yaml подтверждён соответствующей записью в EVIDENCE_LEDGER.md, а не доверять одному реестру.
  Зачем сейчас: Этот разбор нашёл, что premvp.command.production_observation.v1 и premvp.command.release_pipeline.v1 отмечены PROVEN_PRESENT в реестре, но без единой записи в EVIDENCE_LEDGER — реестр может выглядеть завершённым, пока исторический журнал ещё не знает об этом.
  Как ложится на проект: Для каждого заявления по Оси B в будущем цикле требовать и прошедший тест/запись в реестре, и указание, когда возможность реально запускалась или будет запущена — в этом цикле такой планкой стал реальный прогон npm run control-plane:check, выполненный в сессии.
  Что останется в репозитории: Записи axis_b.capabilities этого цикла — каждая содержит конкретную строку use_or_validation_evidence, а не голое описание.

Сравнение: Практика 1 (свежесть состояния) дешёвая и защищает от ошибки уже на следующем шаге планирования; практика 2 (сверка реестра с журналом) — то, что делает заявления по Оси B в каждом будущем цикле заслуживающими доверия, а не декоративными. Практика 1 защищает одну ближайшую задачу, практика 2 — каждый будущий разбор.
Рекомендуемый порядок: сначала FP-1-STATE-FRESHNESS-DISCIPLINE, затем FP-2-REGISTRY-VS-LEDGER-CROSS-CHECK.

## Следующие эксперименты

- Лёгкий детерминированный скрипт проверки свежести состояния (AH-1) можно реализовать быстро, и его вердикт совпадёт с ручным выводом этого разбора — STATE_REFRESH_REQUIRED.
  Границы: Реализовать scripts/control-plane/check-state-freshness.mjs именно на диапазоне a942a0d..origin/main и подтвердить его вывод.
  Что останется: Скрипт плюс тест на фикстурах.
  Считаем удачей: Вердикт скрипта совпадает с ручным выводом этого разбора минимум на двух независимых диапазонах коммитов.
  Останавливаемся, если: Если логике предка/allowlist нужны продуктовые решения, которые нельзя детерминировать — остановиться и оставить проверку ручной.
- Запуск premvp.command.production_observation.v1 на текущем задеплоенном SHA даёт конкретный, доказанный вердикт по блокеру BLK-001 (естественный ночной прогон резерваций), а не оставляет команду неиспользованной.
  Границы: Один явный запуск только для чтения с фиксированным observation_id на живом задеплоенном SHA; полагаться только на встроенную логику возобновления команды, без ручных повторов.
  Что останется: Итоговая запись в EVIDENCE_LEDGER.md с вердиктом (сырой файл чекпоинта остаётся в .gitignore).
  Считаем удачей: Возвращён вердикт, отличный от WAIT, либо формулировка BLK-001 обновлена с датой и доказательствами.
  Останавливаемся, если: При WAIT_INFRASTRUCTURE_RECOVERY — не запускать повторно чаще, чем позволяет собственный интервал возобновления команды.
- Черновики записей EVIDENCE_LEDGER.md для PR#112–#120 по фактам, уже собранным в этом разборе, закрывают пробел в аудите без необходимости новой автоматизации.
  Границы: Один ограниченный проход с проверкой архитектором, составляющий черновики строго по фактам git log/diff, уже зафиксированным в этом цикле — сам разбор не пишет в EVIDENCE_LEDGER.md.
  Что останется: Черновики записей, после принятия архитектором — в EVIDENCE_LEDGER.md.
  Считаем удачей: origin_main_sha в CURRENT_STATE.yaml продвинут state-only bootstrap ближе к текущему концу origin/main, закрывая найденное в этом цикле отставание.
  Останавливаемся, если: Если черновик для какого-то PR требует продуктового решения, а не просто фиксации фактов — направить этот PR на обычное рассмотрение вместо черновика в журнал.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: неизвестно
- стоимость одного проверенного результата: неизвестно
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: 1
- создано переиспользуемых артефактов: 3
- ручных сообщений в CloudCode: 0
- ручных сообщений в Codex: 0
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 0 (полнота сбора — неизвестен).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 1 — ежедневный разбор.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Founder/архитектор рассматривают этот цикл; если он принимается как первый реальный цикл Stage 1, можно предложить архитектору обновить статус Stage 1 в AUTOMATION_ROADMAP.yaml с IMPLEMENTED_NOT_YET_EXERCISED, и в приоритете — AH-6 (запустить premvp.command.production_observation.v1 на текущем деплое) как самый весомый следующий шаг для C1.
