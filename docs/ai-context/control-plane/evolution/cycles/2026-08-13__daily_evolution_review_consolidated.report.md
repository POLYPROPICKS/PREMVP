# Daily Evolution Review

Период: 2026-08-07T11:11:09Z — 2026-08-13T19:16:58Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): продвинулись.
По системе (переиспользуемые возможности): появилась новая переиспользуемая возможность.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- Founder одобрил и зафиксировал в репозитории решение по дорожной карте DIRECT_SERVING_WITH_BOUNDED_BOOTSTRAP (New Contour 7): текущий шаг BOUNDED_PRE_HOOK_SEED, статус FOUNDER_APPROVED_RUNTIME_PENDING, со следующими двумя шагами (CURRENT_OPERATIONAL_COVERAGE_PROOF, NATURAL_WRITER_PROJECTION_PROOF) уже названными явно.
- Раннер бэкфилла для current_signal_pair_serving реально работал в проде и вживую наткнулся на 4 инфраструктурных предела -- watchdog, пул соединений, авторизационный транспорт, Postgres 57014 -- все 4 устранены тем же днём с Contur-ревью по каждому коммиту.
- CONTRACT_A_PLANNING_V1 scored-путь чтения восстановлен после боевого отказа 57014 добавлением одного индекса, создаваемого CONCURRENTLY, плюс регрессионный тест формы DDL/плана запроса -- новый паттерн проверки 'форма индекса соответствует форме запроса' до включения в прод.
- Продюсер дедупликации сигналов переведён с чтения растущего исторического корпуса на текущую (current) проекцию serving, сохраняя insert-first историческую линейность -- прямое применение урока HISTORY_IS_NOT_OPERATIONAL_AUTHORITY из CURRENT_STATE.yaml.operational_lessons_and_deferred_work.
- Опубликован независимый 10-файловый пакет архитектурного/БД-обзора Phase 1 (Planning Serving) -- документирует целевую архитектуру, текущую реализацию с точными ссылками на файл/строку, точные формы SQL-запросов, инвентарь индексов и таймлайн боевых доказательств, без изменения кода/схемы/зависимостей.

Какой следующий проверяемый факт в проде стал возможен: Раз 4 инфраструктурных предела раннера устранены, следующий факт -- критерии phase_1_runtime_acceptance из CURRENT_STATE.yaml (покрытие 100%, PASS-семантика). На снимке от 2026-08-13T12:00 (до PR#164) это ещё WAIT из-за transient Supabase schema-cache ошибки. Снял ли PR#164 эту причину -- не проверялось (БД вне границ)..

Снятые блокеры:
- Раннер бэкфилла мог зависать без ограничения по времени на отдельном запросе -- добавлены 15s heartbeat и 45s отменяемый предел запроса.
- Продолжительный бэкфилл исчерпывал пул сессий БД -- добавлены последовательное закрытие/backoff/переподключение и устойчивое возобновление с чекпоинта.
- Тайм-ауты авторизационного транспорта при чекауте соединения приводили к необработанному сбою -- добавлено восстановление с сохранённой паузой транспорта.
- Скорированное чтение CONTRACT_A_PLANNING_V1 против current_signal_pair_serving отменялось Postgres кодом 57014 (query_canceled) из-за отсутствующего под форму запроса индекса -- восстановлено добавлением частичного индекса CONCURRENTLY плюс регрессионным тестом формы плана.

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- НОВОЕ. Резервируемый раннер бэкфилла с чекпоинтом, watchdog-ограничением запроса, восстановлением пула соединений (backoff/reconnect) и восстановлением авторизационного транспорта -- первый экземпляр паттерна 'долгоживущий раннер против растущей боевой таблицы' в репозитории. (остаётся в репозитории: lib/operations/currentServingBackfillRunner.ts, scripts/current-serving-backfill-runner.ts)
- НОВОЕ. Регрессионный тест формы 'план запроса опирается ровно на нужный частичный индекс' -- формализует то, что раньше проверялось только вручную и было пропущено до боевого отказа 57014. (остаётся в репозитории: tests/feed/currentSignalPairServingPlanningIndex.test.ts, supabase/migrations/20260813193000_current_signal_pair_serving_planning_scored_index.sql)
- УСИЛЕНО (REUSED domain из PR#154). premvp.reviewer.contur_gate.v1 реально вызывался и получал PASS на каждом боевом/инфраструктурном PR периода (5 из 5: #157,158,159,161,164) -- систематически используется, хотя детерминированность вызова (BLK-002) остаётся недоказанной. (остаётся в репозитории: docs/ai-context/control-plane/reviewers/contur-gate-reviewer.yaml)
- REUSED от PR#154 (изначально PR#132/#124). Mission Contract семантический компилятор. (остаётся в репозитории: docs/ai-context/control-plane/PROMPT__PROTOCOL.md, MISSION_CONTRACT.schema.json, scripts/control-plane/mission-compile.mjs)
- REUSED от PR#154 (изначально PR#125). Минимальная инструментация Sentry для продакшен-отладки. (остаётся в репозитории: .agents/skills/polypropicks-production-debug/SKILL.md)
- REUSED от PR#154 (изначально PR#135/#146). Навык независимой проверки для нетривиальных миссий. (остаётся в репозитории: .agents/skills/polypropicks-independent-challenge/SKILL.md)

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- Раннер бэкфилла для current_signal_pair_serving реально работал в проде и вживую наткнулся на 4 инфраструктурных предела -- watchdog, пул соединений, авторизационный транспорт, Postgres 57014 -- все 4 устранены тем же днём с Contur-ревью по каждому коммиту.
- CONTRACT_A_PLANNING_V1 scored-путь чтения восстановлен после боевого отказа 57014 добавлением одного индекса, создаваемого CONCURRENTLY, плюс регрессионный тест формы DDL/плана запроса -- новый паттерн проверки 'форма индекса соответствует форме запроса' до включения в прод.
- Продюсер дедупликации сигналов переведён с чтения растущего исторического корпуса на текущую (current) проекцию serving, сохраняя insert-first историческую линейность -- прямое применение урока HISTORY_IS_NOT_OPERATIONAL_AUTHORITY из CURRENT_STATE.yaml.operational_lessons_and_deferred_work.
- Опубликован независимый 10-файловый пакет архитектурного/БД-обзора Phase 1 (Planning Serving) -- документирует целевую архитектуру, текущую реализацию с точными ссылками на файл/строку, точные формы SQL-запросов, инвентарь индексов и таймлайн боевых доказательств, без изменения кода/схемы/зависимостей.
- НОВОЕ. Резервируемый раннер бэкфилла с чекпоинтом, watchdog-ограничением запроса, восстановлением пула соединений (backoff/reconnect) и восстановлением авторизационного транспорта -- первый экземпляр паттерна 'долгоживущий раннер против растущей боевой таблицы' в репозитории. — 6/6 PASS, перепроверено вживую; 4 боевых восстановления (PR#157,158,159,164) подтверждают паттерн под нагрузкой.
- НОВОЕ. Регрессионный тест формы 'план запроса опирается ровно на нужный частичный индекс' -- формализует то, что раньше проверялось только вручную и было пропущено до боевого отказа 57014. — 1/1 PASS, независимо перезапущено вживую в этой сессии.
- УСИЛЕНО (REUSED domain из PR#154). premvp.reviewer.contur_gate.v1 реально вызывался и получал PASS на каждом боевом/инфраструктурном PR периода (5 из 5: #157,158,159,161,164) -- систематически используется, хотя детерминированность вызова (BLK-002) остаётся недоказанной. — 5 PR-рецептов PASS этого периода, процитированных в телах соответствующих PR.
- REUSED от PR#154 (изначально PR#132/#124). Mission Contract семантический компилятор. — control-plane:check 249/249 PASS, прогнано вживую в этой сессии на текущем origin/main.
- REUSED от PR#154 (изначально PR#125). Минимальная инструментация Sentry для продакшен-отладки. — часть control-plane:check 249/249 PASS, прогнано вживую в этой сессии.
- REUSED от PR#154 (изначально PR#135/#146). Навык независимой проверки для нетривиальных миссий. — часть control-plane:check 249/249 PASS, прогнано вживую в этой сессии.

## Что блокирует следующий шаг

- Критерии phase_1_runtime_acceptance (PRE_HOOK_ELIGIBLE_GAP=0, CURRENT_OPERATIONAL_SERVING_COVERAGE=100%, SERVING_WINNER_SEMANTICS=PASS и т.д.) остаются недоказанными в проде -- последний снимок CURRENT_STATE.yaml явно пишет WAIT, а PR#164 (устраняющий как минимум одну причину WAIT -- 57014) слился ПОСЛЕ этого снимка; повторная проверка WAIT этим разбором не проводилась (доступ к БД вне границ).
- CURRENT_STATE.yaml.main.origin_main_sha (280d35bd, слияние PR#161) -- STATE_REFRESH_REQUIRED: живой origin/main ушёл на 3 слитых PR вперёд (#162, #163, #164), и эти PR трогают миграции/lib-код вне state_bootstrap_allowlist (только CURRENT_STATE.yaml/ARCHITECT_SNAPSHOT.md/EVIDENCE_LEDGER.md). Разрыв заметно уже, чем 36 PR у PR#154, но не закрыт.
- EVIDENCE_LEDGER.md заканчивается записью EV-0020 (2026-08-13T05:30:15Z, соответствует PR#155) -- у 9 слитых после неё PR (#156-#164) нет записи в реестре доказательств.
- Это СЕДЬМОЙ подряд PR цикла Daily Evolution Review (#116, #121, #122, #132, #146, #154 и теперь этот) -- и ни один так и не слит в main. PR#154 (за прошлый период) сам остаётся открытым черновиком на момент этого разбора.
- PR#149 (проектный документ current-serving модели) по-прежнему не слит -- теперь 10 PR реализации (#150,151,153,155-164) ушли вперёд без него.
- tests/contur3/twoStageReservationTiming.test.ts по-прежнему падает на 7 из 14 -- перепроверено вживую в этой сессии на текущем origin/main, без изменений с базовой точки PR#132/#146/#154. Файл всё ещё не подключён ни к одному npm-скрипту.
- Звено S12 (результат на бирже, callback EXECUTED) остаётся UNPROVEN -- нет новых свидетельств за этот период, доступ к Ireland вне границ.
- Сверенный PnL, исполнения, комиссии, расчёт: доказательств нет и не запрашивались; ничего не заявляется.
- Это СЕДЬМОЙ подряд PR цикла Daily Evolution Review -- и ни один так и не слит в main. Способность 'разбор становится постоянной канонической историей репозитория' остаётся недоказанной для своего решающего шага седьмой раз подряд.
- premvp.command.release_pipeline.v1 по-прежнему зарегистрирован ENABLED, но ни разу не был реально вызван -- ни для одного из семи циклов Evolution, включая ровно тот пробный вызов (validate/dry_run -> resume), который PR#154's experiment E1 явно предложил и который так и не состоялся.
- Новый паттерн резервируемого раннера (RECOVERY_AND_CHECKPOINTS) пока подтверждён только на одном-единственном экземпляре (current_signal_pair_serving) -- обобщается ли он на другие долгоживущие боевые раннеры, не проверялось.
- Использовались ли компилятор Mission Contract или навык независимой проверки в реальной небоевой задаче исполнителя (не в самом Evolution-разборе), по-прежнему не проверялось.

## Варианты автоматизации

- Founder одобряет ровно один реальный вызов premvp.command.release_pipeline.v1 в режиме validate/dry_run, затем resume, для ИМЕННО этого консолидированного PR -- впервые проверяя путь черновик -> проверка -> слияние целиком. — делать сейчас.
  Проблема: Это СЕДЬМОЙ подряд PR цикла Daily Evolution Review -- и ни один не слит в main. PR#154's собственный эксперимент E1 прямо предложил один пробный вызов release_pipeline.v1 (validate/dry_run -> resume) именно для себя -- этого не произошло до текущего разбора.
  Что останется в репозитории: docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Когда остановиться: остановиться и эскалировать Founder, если конвейер запросит доступ за пределами уже разрешённого
- Подключить premvp.command.control_plane_reconcile.v1 (уже PROVEN_PRESENT) как обязательный шаг после каждого пакета из 3+ слитых PR, включая одновременное добавление записи EVIDENCE_LEDGER -- не только обновление CURRENT_STATE.yaml. — делать сейчас.
  Проблема: CURRENT_STATE.yaml отстаёт от live origin/main на 3 слитых PR (#162,163,164); EVIDENCE_LEDGER.md отстаёт на 9 слитых PR (#156-164) от своей последней записи EV-0020.
  Что останется в репозитории: docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Когда остановиться: остановиться, если сверка попытается принять расхождение продуктовой фазы без Founder/Architect
- Для любой новой горячей SQL-возможности требовать до боевого включения: (1) тест формы индекса/плана запроса (уже сделано ad hoc в PR#164 -- формализовать), (2) оценку бюджета пула соединений/времени раннера, (3) watchdog/предел по времени по умолчанию для нового долгоживущего раннера. — делать сейчас.
  Проблема: Раскатка current_signal_pair_serving (проекция + раннер бэкфилла) породила 8 последовательных 'слоёв' исправлений за 28 часов (RLS, объём бэкфилла, watchdog, пул соединений, авторизационный транспорт, форма запроса, зависимость от истории, недостающий индекс/57014) -- каждый раз локальные модульные тесты проходили (30/30 в этот период), а проблема ловилась только в проде.
  Что останется в репозитории: предлагается: docs/ai-context/CLAUDE_CODE_EXECUTION_PROTOCOL.md (новый раздел 'hot-path query readiness checklist')
  Когда остановиться: убрать чек-лист, если он будет систематически давать ложные срабатывания на заведомо низкообъёмные изменения
- Зафиксировать в docs/ai-context/VERIFICATION_GATES.md общепроектное (не только для current-serving) правило: любой PR, меняющий или добавляющий горячий SQL-запрос против Supabase, обязан включать тест формы индекса/плана запроса в своём test plan. — сначала продукт.
  Проблема: Postgres 57014 (query_canceled) теперь наблюдался в ДВУХ разных компонентах: планировщике rebalance-соседей (REBALANCE_SIBLING_QUERY_TIMEOUT_57014, из PR#146/#154) и, в этом периоде, скорированном чтении Contract A Planning (CONTRACT_A_PLANNING_SCORED_QUERY_TIMEOUT_57014, PR#164) -- оба раза причина одна и та же: горячий запрос без соответствующего его форме индекса.
  Что останется в репозитории: docs/ai-context/VERIFICATION_GATES.md
  Когда остановиться: убрать правило, если оно даст систематические ложные срабатывания на заведомо холодные (нечастые/маловолюмные) запросы
- REUSED_FROM_PR154 -- требовать в шаблоне PR явную ссылку на статус связанного проектного документа для боевых изменений БД. — сначала продукт.
  Проблема: PR#149 (проектный документ current-serving модели) остаётся неслитым -- теперь 10 PR реализации (#150,151,153,155-161,163,164) ушли вперёд без него, по сравнению с 3 PR на момент PR#154.
  Что останется в репозитории: предлагается: .github/pull_request_template.md
  Когда остановиться: убрать требование при ложных срабатываниях на намеренно независимые документы
- REUSED_FROM_PR154 -- если тема PR порождает 3+ отдельных PR за 24 часа, требовать явное поле 'что было упущено в первой версии' в последнем PR серии. — система позже.
  Проблема: REUSED_FROM_PR154 H8 -- быстрая переделка одной темы в течение суток теперь наблюдалась третий раз подряд (structured-sport-family-carrier у PR#132, mission-2-current-serving у PR#146/#154, и теперь весь цепочка current-serving rollout в этом периоде, см. H3).
  Что останется в репозитории: предлагается: .github/pull_request_template.md
  Когда остановиться: убрать проверку при ложных срабатываниях на намеренно поэтапную разработку
- Зарегистрировать CI_GATE по аналогии с уже запланированным premvp.ci_gate.reviewer_receipt_enforcement: 'premvp.ci_gate.state_freshness_check', падающий, если origin_main_sha в CURRENT_STATE.yaml не является предком последнего слияния в main более чем на N PR. — система позже.
  Проблема: CURRENT_STATE.yaml сейчас формально STATE_REFRESH_REQUIRED (см. axis_a.not_proven) сразу после слияния PR#162,163,164, но это обнаруживается только вручную, этим самым разбором.
  Что останется в репозитории: docs/ai-context/control-plane/AGENT_REGISTRY.yaml (новая запись CI_GATE)
  Когда остановиться: отключить гейт, если он заблокирует не связанные с состоянием PR ложными срабатываниями

## Две практики Founder

- Распознавать луковичную цепочку (onion chain) вместо серии несвязанных багов -- когда 3+ исправления ложатся на одну и ту же границу за ~24-48 часов, останавливаться и спрашивать, какой пограничный инвариант отсутствует, вместо одобрения 'ещё одного точечного патча'.
  Зачем сейчас: Именно это произошло сегодня: 8 последовательных слоёв на одной границе (current_signal_pair_serving) за 28 часов, включая настоящий боевой отказ 57014. Каждое отдельное исправление было правильным, но никто не остановился спросить 'чего вообще не хватает системно', пока это не сделал этот разбор.
  Как ложится на проект: При просмотре следующей серии из 3+ PR на одну тему за короткое время явно спрашивать: это независимые баги или один и тот же непроверенный пограничный инвариант проявляется слой за слоем? См. H3/H4 выше как готовый пример такого вопроса.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/cycles/2026-08-13__daily_evolution_review_consolidated.json
- Доводить уже принятое решение до конца в тот же рабочий день, а не давать ему повторяться в виде рекомендации из цикла в цикл.
  Зачем сейчас: Это третий цикл подряд (PR#132 H5 -> PR#146 -> PR#154 experiment E1), где предлагается один и тот же пробный вызов release_pipeline.v1, и третий раз подряд он не происходит. Разрыв CURRENT_STATE, наоборот, сузился (36 PR -> 3 PR) именно потому, что кто-то реально стал чаще запускать сверку -- это доказательство, что 'довести до конца' работает, когда происходит.
  Как ложится на проект: Когда Evolution-разбор прямо называет одно конкретное действие (сейчас: H1 -- пробный вызов release_pipeline.v1 для этого PR), делать именно это в течение того же рабочего дня, используя уже сузившийся разрыв CURRENT_STATE как доказательство, что доведение до конца возможно.
  Что останется в репозитории: docs/ai-context/control-plane/CURRENT_STATE.yaml

Сравнение: Первая практика дешева и применяется в одиночку при чтении любого следующего отчёта за пару минут -- достаточно спросить один вопрос. Вторая требует настоящего решения Founder и напрямую разблокирует ценность всей возможности Evolution, простаивающей семь циклов подряд -- поэтому она приоритетнее, хотя обе можно делать параллельно.
Рекомендуемый порядок: сначала P2, затем P1.

## Следующие эксперименты

- REUSED_FROM_PR154, ЕЩЁ НЕ ИСПОЛНЕНО. Если Founder одобрит ровно один вызов premvp.command.release_pipeline.v1 в режиме validate/dry_run, затем resume, для этого консолидированного PR, можно будет впервые за семь циклов подряд узнать, работает ли путь черновик -> проверка -> слияние целиком.
  Границы: Один запуск, только режимы validate/dry_run затем resume, только новые файлы этого PR, без прямого push в main.
  Что останется: docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Считаем удачей: Этот PR слит в main через зарегистрированный конвейер, результат зафиксирован записью EVIDENCE_LEDGER.
  Останавливаемся, если: Если конвейер запросит доступ за пределами уже разрешённого -- остановиться и эскалировать Founder.
- Если формализовать тест формы индекса/плана запроса (уже сделанный ad hoc в PR#164) как обязательный шаблон для следующей новой горячей SQL-читающей возможности, класс отказов 57014 не повторится в третьем компоненте.
  Границы: Один новый вспомогательный тест-хелпер по образцу tests/feed/currentSignalPairServingPlanningIndex.test.ts, применённый только к следующему PR, добавляющему новый горячий запрос -- без общего блокирующего CI-гейта на все существующие запросы сразу.
  Что останется: tests/feed/currentSignalPairServingPlanningIndex.test.ts (уже существует как образец) + новый переиспользуемый хелпер
  Считаем удачей: Хотя бы один следующий PR с новым горячим SQL-запросом ссылается на этот хелпер в своём test plan без боевого 57014 после слияния.
  Останавливаемся, если: Если хелпер даст систематические ложные срабатывания на заведомо холодные запросы -- оставить его необязательным образцом, не гейтом.
- REUSED_FROM_PR154, ЧАСТИЧНО ИСПОЛНЕНО (разрыв CURRENT_STATE сузился с 36 до 3 PR, но EVIDENCE_LEDGER отстаёт на 9). Один Architect-одобренный запуск premvp.command.control_plane_reconcile.v1 закроет оставшийся разрыв в 3 PR CURRENT_STATE и 9 PR EVIDENCE_LEDGER одновременно.
  Границы: Один запуск, только канонические артефакты control-plane, без изменения roadmap_phase, current_value_step или закрытых блокеров.
  Что останется: docs/ai-context/control-plane/CURRENT_STATE.yaml
  Считаем удачей: origin_main_sha в CURRENT_STATE.yaml становится проверенным предком текущего origin/main, и EVIDENCE_LEDGER.md получает записи для PR#156-164 без ручного редактирования JSON.
  Останавливаемся, если: Если сверка попытается принять любое расхождение продуктовой фазы или закрытых блокеров без Founder/Architect -- остановиться.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: 6
- стоимость одного проверенного результата: нет данных
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: 4
- создано переиспользуемых артефактов: 6
- ручных сообщений в CloudCode: 0
- ручных сообщений в Codex: 0
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 0 (полнота сбора — неизвестен).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 1 — ежедневный разбор.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Founder подтверждает, что этот цикл канонический (PR#154 закрывается как superseded, как ранее #132/#146), и одобряет пробный вызов release-конвейера для слияния (H1/E1). Отдельно: рассмотреть H3/H4 (чек-лист для новых горячих SQL-путей) до следующей возможности, трогающей Supabase.
