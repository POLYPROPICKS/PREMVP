# Daily Evolution Review

Период: 2026-08-10T00:00:00Z — 2026-08-11T20:49:28Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): недостаточно доказательств.
По системе (переиспользуемые возможности): появилась новая переиспользуемая возможность.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- 22 PR (#123-#145) влились в origin/main за ~44 часа, почти все — в зоне открытого шага C1 (CONTUR3_QUEUE_AUTHORITY): сравнение времени, авторитет provider-ID, распространение structured-sport, фикс таймаута запроса, политика живого распределения и вывод identity очереди для Ирландии.
- Появилось новое регрессионное покрытие overnight-preflight (PR#141 — проверка пути после резервирования, PR#145 — согласование фикстуры неизменяемой очереди), которое сужает разрыв до всё ещё открытого требования BLK-001 (доказанный ночной прогон), но это тестовое покрытие, а не сам боевой прогон.

Какой следующий проверяемый факт в проде стал возможен: Пройдёт ли естественный ночной прогон (Planning -> Reservation -> Final Identity -> неизменяемая Queue) чисто поверх нового фикса identity очереди для Ирландии (PR#143/#144) — это теперь проверяемо, но не проверено в этом разборе: нужен доступ к продакшену и базе данных, которого у этого разбора нет..

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- 25-секционную модель промпта заменили на семантический компилятор Mission Contract и генератор ChatGPT project-package. (остаётся в репозитории: scripts/control-plane/lib/mission-compile.mjs)
- Появилась способность диагностики прода: инструментирование Sentry зарегистрировано в AGENT_REGISTRY.yaml, с явным запретом на диагностику только по тексту ошибки и на включение PII/трейсинга по умолчанию. (остаётся в репозитории: docs/ai-context/control-plane/AGENT_REGISTRY.yaml)
- Зарегистрирован навык независимой проверки: ограниченная роль 'адвоката дьявола' для нетривиальных миссий, подключённая в AGENT_REGISTRY.yaml и ROUTING_AND_PIPELINES.yaml. (остаётся в репозитории: .agents/skills/polypropicks-independent-challenge/SKILL.md)

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- 22 PR (#123-#145) влились в origin/main за ~44 часа, почти все — в зоне открытого шага C1 (CONTUR3_QUEUE_AUTHORITY): сравнение времени, авторитет provider-ID, распространение structured-sport, фикс таймаута запроса, политика живого распределения и вывод identity очереди для Ирландии.
- 25-секционную модель промпта заменили на семантический компилятор Mission Contract и генератор ChatGPT project-package. — 30 тестов в 2 наборах (missionContract, chatgptProjectPackage) — перезапущены и прошли зелёными в этой сессии.
- Появилась способность диагностики прода: инструментирование Sentry зарегистрировано в AGENT_REGISTRY.yaml, с явным запретом на диагностику только по тексту ошибки и на включение PII/трейсинга по умолчанию. — tests/control-plane/sentryProductionDebug.test.mjs, 4/4 прошли — перезапущены и подтверждены зелёными в этой сессии.
- Зарегистрирован навык независимой проверки: ограниченная роль 'адвоката дьявола' для нетривиальных миссий, подключённая в AGENT_REGISTRY.yaml и ROUTING_AND_PIPELINES.yaml. — tests/control-plane/independentChallengeSkill.test.mjs, 8/8 прошли — перезапущены и подтверждены зелёными в этой сессии.

## Что блокирует следующий шаг

- Остаются ли открытыми блокеры BLK-001..BLK-004 из CURRENT_STATE.yaml, закрылись ли, или появился новый риск после фиксов этого периода — CURRENT_STATE.yaml не обновлялся с 2026-08-07T11:11:09Z, это четыре дня и около 35 мёржей позади живого origin/main, а этот разбор не трогает продакшен и базу, чтобы проверить напрямую.
- Понадобился ли PR#144 потому что фикс identity очереди в PR#143 был неполным, или потому что работу намеренно разделили на два PR — описание PR не изучалось, чтобы различить эти варианты.
- Сверенный PnL, исполнения, комиссии или расчёт за период — таких данных нет и они не запрашивались; ничего не заявляется.
- Доказана ли эффективность фиксов этого периода в проде — подтверждено только их присутствие в origin/main через git ancestry.
- Использовались ли компилятор Mission Contract или навык независимой проверки в реальной задаче исполнителя — оба доказанно присутствуют и покрыты тестами, но ещё не доказано применение в бою.
- Ловил ли уже Sentry-контур реальную продовую проблему — пока такого случая не было.
- Это первый цикл Stage 1, поэтому сравнивать преемственность способностей между циклами пока не с чем.

## Варианты автоматизации

- Лёгкий экспортёр на каждой поверхности (хук начала/конца сессии CloudCode; чтение лога на стороне Codex), который дописывает одну JSON-строку на каждое отправленное Founder-сообщение в файл в формате Evolution input-bundle. — делать сейчас.
  Проблема: Коллектор операторских действий Stage 1 существует с PR#109, но ни разу не получал реальный входной пакет. Счётчик Founder-действий в этом цикле — это пол в 0, а не измеренные данные, просто потому что нет источника, который фиксирует сообщения Founder в CloudCode/Codex.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/input-bundles/ (потребитель уже есть в scripts/control-plane/lib/operator-actions.mjs — не хватает только источника захвата)
  Когда остановиться: Остановиться, если потребуется читать содержимое сообщения сверх типа/времени/поверхности.
- Подключить уже существующий scripts/control-plane/reconcile-control-plane.mjs как регулярную или срабатывающую на мёрж проверку, которая помечает CURRENT_STATE как STATE_REFRESH_REQUIRED по правилу stale_when, уже описанному в самом CURRENT_STATE.yaml, вместо того чтобы полагаться на следующий Daily Evolution Review. — делать сейчас.
  Проблема: CURRENT_STATE.yaml.updated_at — 2026-08-07T11:11:09Z. Живой origin/main с тех пор продвинулся примерно на 35 мёржей, а в EVIDENCE_LEDGER.md нет ни одной соответствующей записи; расхождение обнаружил случайно этот разбор, а не сам контур.
  Что останется в репозитории: scripts/control-plane/reconcile-control-plane.mjs (расширить триггер запуска, не саму логику)
  Когда остановиться: Останавливать не нужно — это чисто дополнительное обнаружение, ничего не меняющее само по себе.
- Небольшой валидатор, который обнаруживает повторное использование одного имени ветки в PR за период и фиксирует это как связь-исправление, вместо ручного разбора. — система позже.
  Проблема: PR#129, #130 и #131 трижды использовали одно и то же имя ветки (structured-sport-family-carrier) для отдельных мёржей, и PR#143/#144 сделали то же самое для restore-ireland-queue-contract — без отслеживаемой связи, что один PR исправляет другой.
  Что останется в репозитории: предлагается: scripts/control-plane/lib/ (новый небольшой модуль) плюс поле схемы у записей confirmed_changes
  Когда остановиться: Отказаться, если объём PR в большинстве периодов слишком мал, чтобы сигнал имел смысл.
- Это не действие контура Evolution, а предложение по продуктовому коду: закреплённая контрактная матрица тестов identity-authority, которой должны соответствовать новые границы provider/queue/feed. — сначала продукт.
  Проблема: 5 исправлений менее чем за 3 часа (PR#128-#131, 41c9b1c) боролись с одним и тем же дефектом structured-sport/family-carrier; похожий фикс provider-identity (PR#127) и фикс identity очереди для Ирландии (PR#143/#144) повторились позже — все про 'какое поле здесь авторитетно' на критическом пути C1.
  Что останется в репозитории: предлагается: tests/contur3/identityAuthorityContract.test.ts (пока не существует)
  Когда остановиться: Остановиться, если фикстуру контракта придётся постоянно переписывать — это сигнал неверной абстракции.
- Требовать completion envelope как завершающий шаг исполнительской сессии по отслеживаемой миссии, с сохранением в отслеживаемой директории. — система позже.
  Проблема: docs/ai-context/control-plane/COMPLETION_ENVELOPE.schema.json и scripts/control-plane/validate-completion-envelope.mjs существуют с PR#77/#109, но ни одного реального экземпляра completion envelope в репозитории нет — inputs.completion_envelope_ids в этом цикле неизбежно пуст.
  Что останется в репозитории: предлагается: docs/ai-context/control-plane/completions/ (пока не существует)
  Когда остановиться: Останавливать не нужно.

## Две практики Founder

- Требовать доказательство результата в реальном контуре, а не считать факт мёржа в main завершением
  Зачем сейчас: За 44 часа в main влилось 22 PR прямо в зоне текущего открытого шага C1, но ни один из открытых блокеров (BLK-001..004) не подтверждён закрытым — потому что подтверждение требует боевого прогона, а не факта мёржа.
  Как ложится на проект: Перед тем как считать C1 ближе к готовности, явно спросить: 'какой прод-факт это доказывает прямо сейчас' — и принять ответ 'ещё никакой' как валидный ответ, а не как повод считать работу законченной.
  Что останется в репозитории: docs/ai-context/control-plane/CURRENT_STATE.yaml (open_blockers)
- Держать CURRENT_STATE.yaml и EVIDENCE_LEDGER.md синхронизированными с реальным origin/main, а не только после Evolution-задач
  Зачем сейчас: CURRENT_STATE.yaml не обновлялся с 7 августа, а за это время в main влилось около 35 PR — расхождение обнаружил только этот разбор, а не сам контур.
  Как ложится на проект: После заметного пакета мёржей (не только Evolution-цикла) запускать существующий scripts/control-plane/reconcile-control-plane.mjs, а не ждать, пока рассинхронизация станет отдельной находкой следующего разбора.
  Что останется в репозитории: docs/ai-context/control-plane/EVIDENCE_LEDGER.md

Сравнение: Первая практика дешевле и защищает бизнес-достоверность прямо сейчас — она не требует новых инструментов, только привычки задавать вопрос. Вторая требует регулярного действия (запуск существующего скрипта) и защищает системную достоверность, но зависит от того, что первая уже закреплена.
Рекомендуемый порядок: сначала P1, затем P2.

## Следующие эксперименты

- Если запускать scripts/control-plane/reconcile-control-plane.mjs после каждого дня с 5+ мёржами, CURRENT_STATE.yaml не будет отставать больше чем на 1-2 дня.
  Границы: Следующие 3 календарных дня с активным мёржем, только чтение и запись CURRENT_STATE.yaml/EVIDENCE_LEDGER.md через существующий скрипт.
  Что останется: docs/ai-context/control-plane/EVIDENCE_LEDGER.md (новые записи о сверке)
  Считаем удачей: На следующем Daily Evolution Review CURRENT_STATE.yaml отстаёт не больше чем на 1-2 дня от origin/main.
  Останавливаемся, если: Если сверка вручную занимает больше времени, чем экономит — остановиться и вернуться к разбору по факту, а не по расписанию.
- Если фиксировать в одном месте, когда PR повторно использует то же имя ветки (как structured-sport-family-carrier и restore-ireland-queue-contract), rework_count можно будет считать без ручного разбора git log.
  Границы: Следующие 2 цикла Daily Evolution Review, только чтение git-истории.
  Что останется: docs/ai-context/control-plane/evolution/cycles/ (следующий цикл ссылается на этот)
  Считаем удачей: Следующий цикл получает rework_count без ручного просмотра веток.
  Останавливаемся, если: Если совпадение имён веток не даёт надёжного сигнала (слишком много ложных срабатываний) — остановиться.
- Если явно потребовать один completion envelope на исполнительскую сессию, к третьему циклу completion_envelope_ids перестанет быть пустым.
  Границы: Одна выбранная Architect-сессия в качестве пилота, без изменения продукта.
  Что останется: docs/ai-context/control-plane/completions/ (появится при первом реальном envelope)
  Считаем удачей: Хотя бы один настоящий completion envelope закоммичен и проходит validate-completion-envelope.mjs.
  Останавливаемся, если: Если оформление envelope занимает заметно больше времени, чем сама задача — остановиться и упростить схему, а не заставлять Founder/executor подгонять работу под форму.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: 5
- стоимость одного проверенного результата: нет данных
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: неизвестно
- создано переиспользуемых артефактов: 9
- ручных сообщений в CloudCode: 0
- ручных сообщений в Codex: 0
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 0 (полнота сбора — неизвестен).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 1 — ежедневный разбор.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Следующий разбор: подтвердить, отставание CURRENT_STATE.yaml сократилось или нет, и появился ли хотя бы один реальный источник Founder-действий вместо нулевого потолка. Governor (Routine 2) получает 5 гипотез, 9 семей дефектов и первый реальный операционный срез для сравнения со следующими циклами.
