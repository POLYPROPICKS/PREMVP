# Daily Evolution Review

Период: 2026-08-07T11:11:09Z — 2026-08-12T15:44:18Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): продвинулись.
По системе (переиспользуемые возможности): появилась новая переиспользуемая возможность.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- Канонический манифест спины (PR#148) фиксирует по прямому распоряжению Founder: прод-прогон 11-12 августа прошёл весь путь Planning -> Reservation -> Queue -> Ireland-декодирование -> callback CLAIM -> Ireland SENDING (попытка отправки на биржу). 8 из 12 звеньев цепочки признаны доказанными в проде -- самое глубокое место всей цепочки на сегодня.
- Ireland отклоняла живые Queue-инструкции с ошибкой UNSUPPORTED_SIDE, потому что поле side у PREMVP несёт выбор исхода (YES/NO), а не действие ордера CLOB (BUY/SELL). PR#147 добавил отдельное поле execution_side=‘BUY‘ (эта очередь только на вход, пути на выход нет нигде в Contur3) плюс queue_row_id и signal_pair_id, не трогая существующее поле side.
- Манифест S9 прямо подтверждает: сбой декодирования UNSUPPORTED_SIDE, из-за которого появился PR#147, для прогона 11-12 августа считается устранённым -- то есть именно этот блокер снят и подтверждён тем же прод-прогоном, который его же и обнаружил.
- Contract A Planning больше не читает растущий исторический корпус generated_signal_pairs напрямую -- PR#150/151/153 ввели ограниченную проекцию current_signal_pair_serving (детерминированная замена, идемпотентный бэкфилл, RLS на запись), снижая нагрузку на путь чтения без изменения исторической авторитетности.

Какой следующий проверяемый факт в проде стал возможен: Раз Ireland SENDING (попытка отправки на биржу) признана доказанной, следующий проверяемый факт -- результат на самой бирже (S12: исполнение/отказ) и совпадающий callback со статусом EXECUTED. Манифест сам пишет, что это ещё не проверено..

Снятые блокеры:
- Ошибка декодирования UNSUPPORTED_SIDE на стороне Ireland, мешавшая очереди дойти дальше наблюдения/декодирования, устранена и подтверждена тем же прод-прогоном 11-12 августа, который её же и выявил.

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- REUSED from PR#132 (2026-08-10). Mission Contract семантический компилятор заменил 25-секционную модель промпта. (остаётся в репозитории: docs/ai-context/control-plane/PROMPT__PROTOCOL.md, MISSION_CONTRACT.schema.json, scripts/control-plane/mission-compile.mjs)
- REUSED from PR#132/PR#146. Минимальная инструментация Sentry для продакшен-отладки плюс отдельный навык production-debug и узкая политика доступа. (остаётся в репозитории: .agents/skills/polypropicks-production-debug/SKILL.md)
- REUSED from PR#146. Навык независимой проверки ('адвокат дьявола') для нетривиальных миссий, подключённый в AGENT_REGISTRY.yaml и ROUTING_AND_PIPELINES.yaml. (остаётся в репозитории: .agents/skills/polypropicks-independent-challenge/SKILL.md)
- НОВОЕ. Манифест спины + матрица контрактов + реестр устаревших путей впервые разделяют 'символ есть в коде' от 'звено реально пронесло событие в проде'. (остаётся в репозитории: docs/ai-context/control-plane/CANONICAL_VERTICAL_SPINE_MANIFEST.yaml (+ CROSS_SYSTEM_CONTRACT_MATRIX.yaml, LEGACY_SUPERSEDED_INVENTORY.yaml))
- НОВОЕ в этом цикле. Переиспользуемый паттерн 'текущая проекция поверх растущего исторического корпуса': детерминированная замена, идемпотентный бэкфилл с чекпоинтом, явный статус ожидания вместо тихого отката на неограниченное чтение истории. (остаётся в репозитории: lib/feed/currentSignalPairServing.ts (+ migration 20260812_current_signal_pair_serving.sql))

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- Ireland отклоняла живые Queue-инструкции с ошибкой UNSUPPORTED_SIDE, потому что поле side у PREMVP несёт выбор исхода (YES/NO), а не действие ордера CLOB (BUY/SELL). PR#147 добавил отдельное поле execution_side=‘BUY‘ (эта очередь только на вход, пути на выход нет нигде в Contur3) плюс queue_row_id и signal_pair_id, не трогая существующее поле side.
- Contract A Planning больше не читает растущий исторический корпус generated_signal_pairs напрямую -- PR#150/151/153 ввели ограниченную проекцию current_signal_pair_serving (детерминированная замена, идемпотентный бэкфилл, RLS на запись), снижая нагрузку на путь чтения без изменения исторической авторитетности.
- REUSED from PR#132 (2026-08-10). Mission Contract семантический компилятор заменил 25-секционную модель промпта. — control-plane:check 249/249 PASS, включая проверки этой схемы, прогнано вживую в этой сессии на текущем origin/main.
- REUSED from PR#132/PR#146. Минимальная инструментация Sentry для продакшен-отладки плюс отдельный навык production-debug и узкая политика доступа. — tests/control-plane/sentryProductionDebug.test.mjs, часть control-plane:check 249/249 PASS, прогнано вживую в этой сессии.
- REUSED from PR#146. Навык независимой проверки ('адвокат дьявола') для нетривиальных миссий, подключённый в AGENT_REGISTRY.yaml и ROUTING_AND_PIPELINES.yaml. — tests/control-plane/independentChallengeSkill.test.mjs, часть control-plane:check 249/249 PASS, прогнано вживую в этой сессии.
- НОВОЕ. Манифест спины + матрица контрактов + реестр устаревших путей впервые разделяют 'символ есть в коде' от 'звено реально пронесло событие в проде'. — control-plane:check 249/249 PASS, прогнано вживую в этой сессии; регистрация подтверждена в ARCHITECT_CONTROL_PLANE.yaml.
- НОВОЕ в этом цикле. Переиспользуемый паттерн 'текущая проекция поверх растущего исторического корпуса': детерминированная замена, идемпотентный бэкфилл с чекпоинтом, явный статус ожидания вместо тихого отката на неограниченное чтение истории. — 6/6 тестов независимо перезапущено вживую в этой сессии.

## Что блокирует следующий шаг

- BLK-001 в CURRENT_STATE.yaml формально остаётся OPEN: сам CURRENT_STATE.yaml не обновлялся с 2026-08-07T11:11:09Z (state_version 14) и не сверялся с доказательством прогона 11-12 августа -- это пробел в учёте control-plane, а не сомнение в самом доказательстве.
- Доказательство прогона 11-12 августа само по себе -- это FOUNDER_ACCEPTED_EXTERNAL_CHECKPOINT (прямое распоряжение Founder), а не независимо перепроверенное этой или любой другой сессией через живые git/HTTP/DB-чтения: доступ к Ireland вне границ репозитория для всех исполнителей. Разница между 'доказано в проде' и 'доказано ЖИВЫМ чтением этой сессией' сохраняется явно.
- Звено S12 (результат на бирже, совпадающий callback, статус EXECUTED) остаётся UNPROVEN -- манифест сам говорит, что нужен отдельный диагностический прогон после этого.
- tests/contur3/twoStageReservationTiming.test.ts по-прежнему падает на 7 из 14 -- перепроверено вживую в этой сессии на текущем origin/main, без изменений с базовой точки 2026-08-07 и с проверки PR#132. Файл всё ещё не подключён ни к одному npm-скрипту, поэтому ни один гейт не может его поймать.
- EVIDENCE_LEDGER.md всё ещё заканчивается записью EV-0019 (2026-08-07) -- ни у одного из 36 слитых PR с момента базовой точки (#111-#153, за вычетом никогда не слитых #116/121/122/132/146/149 и закрытого без слияния #152) нет записи в реестре доказательств или completion envelope.
- Сверенный PnL, исполнения, комиссии, расчёт: доказательств нет и не запрашивались; ничего не заявляется.
- PR#150/151/153 реализовали проекцию до того, как её собственный проектный документ (PR#149) был слит -- то есть боевой код обогнал документацию, которая должна была быть его основанием; не проверялось, было ли это осознанным решением.
- Это ШЕСТОЙ подряд PR цикла Daily Evolution Review (#116, #121, #122, #132, #146 и теперь этот) -- и ни один так и не слит в main. Способность 'разбор становится постоянной историей репозитория' остаётся недоказанной для своего решающего шага шестой раз подряд, хотя сбор и оценка работают каждый раз.
- premvp.command.release_pipeline.v1 по-прежнему зарегистрирован ENABLED в живом AGENT_REGISTRY.yaml, но ни разу не был реально вызван для слияния ни одного из шести циклов Evolution.
- Использовались ли компилятор Mission Contract или навык независимой проверки в реальной небоевой задаче исполнителя (не в самом Evolution-разборе) -- не проверялось в этом цикле.
- Новая проекция current_signal_pair_serving не проверена на реальных прод-данных этой сессией (доступ к БД вне границ этого разбора) -- проверены только модульные тесты на фикстурах.

## Варианты автоматизации

- Founder одобряет ровно один реальный вызов premvp.command.release_pipeline.v1 (уже ENABLED) в режиме validate/dry_run, затем resume, для ИМЕННО этого консолидированного PR -- первая настоящая проверка пути черновик -> проверка -> слияние целиком, прежде чем создавать седьмой цикл. — делать сейчас.
  Проблема: Это ШЕСТОЙ подряд PR цикла Daily Evolution Review (#116 2026-08-07, #121 2026-08-08, #122 2026-08-09, #132 2026-08-10, #146 2026-08-11, и теперь этот, 2026-08-12) -- и ни один так и не был слит в main. Три из них (#116, #121, #122) закрыты как superseded; #132 и #146 закрываются этим же циклом; сам этот цикл тоже уходит в PENDING_RESUMABLE, а не в main.
  Что останется в репозитории: docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Когда остановиться: остановиться и эскалировать Founder, если конвейер запросит доступ за пределами уже разрешённого (прямой push, база данных, кросс-репозиторные действия)
- Подключить уже существующий scripts/control-plane/reconcile-control-plane.mjs как регулярную проверку после заметного пакета мёржей, помечающую CURRENT_STATE как STATE_REFRESH_REQUIRED по правилу stale_when, уже описанному в самом файле. — делать сейчас.
  Проблема: CURRENT_STATE.yaml (state_version 14, updated_at 2026-08-07T11:11:09Z) не обновлялся за весь период. С момента базовой точки в main влилось 36 PR (#111-#153 за вычетом никогда не слитых), и ни один из них не отражён в CURRENT_STATE.yaml. Разрыв рос с 9 (PR#121) до 18 (PR#132) до ~35 (PR#146) и теперь 36.
  Что останется в репозитории: scripts/control-plane/reconcile-control-plane.mjs (расширить триггер запуска, не саму логику)
  Когда остановиться: останавливать не нужно -- это чисто дополнительное обнаружение
- Лёгкий экспортёр на каждой поверхности (хук начала/конца сессии CloudCode; чтение лога на стороне Codex), дописывающий одну JSON-строку на каждое отправленное Founder-сообщение в формате Evolution input-bundle. — делать сейчас.
  Проблема: Коллектор операторских действий Stage 1 существует с PR#109, но ни разу не получал реальный входной пакет за шесть циклов подряд. Из 36 слитых PR только один коммит несёт явную ссылку на сессию Claude Code; остальные claude-коммиты несут только соавторство без сессии, а codex-коммиты не несут идентификатора сессии вовсе.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/input-bundles/ (потребитель уже есть в scripts/control-plane/lib/operator-actions.mjs -- не хватает только источника захвата)
  Когда остановиться: остановиться, если понадобится читать содержимое сообщения сверх типа/времени/поверхности
- Небольшой валидатор, обнаруживающий повторное использование одного имени ветки в PR за период и фиксирующий это как связь-исправление вместо ручного разбора. — система позже.
  Проблема: PR#147 и PR#148 слиты с одной и той же ветки claude/ireland-queue-contract-fields по двум разным темам (поля контракта очереди; затем канонический манифест спины). PR#150, PR#151 и закрытый без слияния PR#152 использовали одну ветку codex/mission-2-current-serving; PR#153 -- новую ветку тем же утром для того же логического продолжения работы.
  Что останется в репозитории: предлагается: scripts/control-plane/lib/ (новый небольшой модуль) плюс поле схемы у записей confirmed_changes
  Когда остановиться: отказаться, если объём PR в большинстве периодов слишком мал для значимого сигнала
- Небольшой скрипт, сопоставляющий каждый SHA в диапазоне с его PR через merge-commit-структуру и/или GitHub API один раз, кэширующий результат как каноническое доказательство -- вместо повторной ручной археологии в каждом цикле. — система позже.
  Проблема: Коммиты bceb1214 и 63cb4053 (часть PR#148, слитого merge-commit, а не squash) не несут суффикса номера PR в сообщении -- в отличие от PR#150/151/153 (squash), чьи сообщения такой суффикс несут автоматически. Потребовались отдельные вызовы GitHub API в этом цикле, чтобы подтвердить, что это не прямой push в обход ревью.
  Что останется в репозитории: предлагается: scripts/control-plane/lib/pr-attribution.mjs (пока не существует)
  Когда остановиться: отказаться, если GitHub API окажется недостаточно для полного покрытия (например приватные форки)
- Добавить один именованный npm-скрипт для этого файла (или всего tests/contur3/), не исправляя сам падающий тест и не встраивая его в блокирующий гейт. — делать сейчас.
  Проблема: tests/contur3/twoStageReservationTiming.test.ts падает 7 из 14 -- перепроверено вживую в этой сессии на текущем origin/main, без изменений с базовой точки периода (2026-08-07) и с проверки PR#132 (2026-08-10). Файл по-прежнему не подключён ни к одному npm-скрипту.
  Что останется в репозитории: package.json
  Когда остановиться: если подключение к общему гейту заблокирует не связанные PR из-за уже существующего провала -- оставить скрипт отдельным
- Не техническая автоматизация, а предложение процесса: для боевых изменений БД с отдельным проектным документом требовать слияние проектного документа до или вместе с реализацией, либо явную перекрёстную ссылку в теле PR реализации на всё ещё открытый проектный PR. — сначала продукт.
  Проблема: PR#150/151/153 реализовали новую боевую проекцию БД (current_signal_pair_serving) и мигрировали Contract A Planning на неё до того, как проектный документ PR#149 (docs(control-plane): design current Signal Pair serving model), который эта работа же и реализует, был слит в main -- PR#149 остаётся открытым на момент этого цикла.
  Что останется в репозитории: предлагается: .github/pull_request_template.md (поле 'design doc status')
  Когда остановиться: убрать требование, если оно даст ложные срабатывания на намеренно независимые проектные документы
- Расширить простую проверку из H7/PR#132 H7: если ветка/тема PR за последние 24 часа порождает 3+ отдельных PR, требовать явное поле 'что было упущено в первой версии' в последнем PR серии. — система позже.
  Проблема: 4 PR (#150, #151, закрытый без слияния #152, #153) потребовались на одну и ту же новую возможность (current_signal_pair_serving) за 23 минуты: сначала функция, затем найденный тут же пробел безопасности (RLS), затем брошенная дублирующая попытка, затем реальное ограничение бэкфилла.
  Что останется в репозитории: .github/pull_request_template.md или ROUTING_AND_PIPELINES.yaml
  Когда остановиться: убрать проверку, если она даст ложные срабатывания на намеренно поэтапную разработку

## Две практики Founder

- Отличать 'доказано Founder-директивой' от 'доказано независимым живым чтением этой же сессией' -- и не смешивать их в одну строку
  Зачем сейчас: Главная находка цикла (8 звеньев Planning->Ireland SENDING доказаны в проде) целиком опирается на прямое распоряжение Founder -- ни одна сессия не перепроверяла Ireland-сторону напрямую (доступ вне границ). При этом падения теста синхронизации времени были лично перепроверены вживую в этой сессии. Уровень доказательности у этих двух фактов разный.
  Как ложится на проект: При чтении любого следующего отчёта явно спрашивать: 'кто именно это проверил и как' -- прямое распоряжение Founder, git-археология этой сессии, или чужое непроверенное заявление -- и записывать класс доказательства соответственно, а не одним словом 'доказано'.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/cycles/2026-08-12__daily_evolution_review_consolidated.json
- Доводить уже принятое системное решение до конца, а не давать ему тихо накапливаться в виде очередной рекомендации
  Зачем сейчас: PR#132 (2026-08-10) уже прямо рекомендовал: выбрать один канонический PR цикла Evolution и закрыть остальные. Этого не произошло -- вместо этого появился PR#146 (2026-08-11), который тоже не был закрыт или слит, и теперь появляется третий по счёту такой цикл. Три раза подряд одна и та же рекомендация повторяется, не будучи исполненной.
  Как ложится на проект: Когда Architect/Evolution-разбор прямо называет одно конкретное действие (в данном случае: закрыть #132 и #146, одобрить пробный вызов release_pipeline.v1 для ровно одного PR), делать именно это в течение того же рабочего дня, а не откладывать до следующего разбора, который снова назовёт ту же рекомендацию.
  Что останется в репозитории: docs/ai-context/control-plane/CURRENT_STATE.yaml

Сравнение: Первая практика дешева и не требует решения -- применяется в одиночку при чтении любого отчёта за пару минут. Вторая требует настоящего действия Founder и напрямую разблокирует ценность всей возможности Evolution, которая простаивает шестой цикл подряд -- поэтому она приоритетнее, даже если делается второй по порядку.
Рекомендуемый порядок: сначала P1, затем P2.

## Следующие эксперименты

- Если Founder одобрит ровно один вызов premvp.command.release_pipeline.v1 (уже ENABLED) в режиме validate/dry_run, затем resume, для этого консолидированного PR, можно будет впервые за шесть циклов подряд узнать, работает ли путь черновик -> проверка -> слияние целиком, или упирается в конкретное препятствие.
  Границы: Один запуск, только режимы validate/dry_run затем resume, только новые файлы этого PR (cycle JSON, report.md, input bundle), без прямого push в main (запрещено самим контрактом команды).
  Что останется: docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Считаем удачей: Этот PR слит в main через зарегистрированный конвейер, результат зафиксирован записью EVIDENCE_LEDGER.
  Останавливаемся, если: Если конвейер запросит доступ за пределами уже разрешённого (прямой push, база данных, кросс-репозиторные действия) -- остановиться и эскалировать Founder.
- Если подключить tests/contur3/twoStageReservationTiming.test.ts к именованному npm-скрипту, его текущие 7 из 14 провалов перестанут быть невидимыми для будущих PR, трогающих путь синхронизации времени C1.
  Границы: Только добавление одной строки в package.json, без исправления самого падающего теста.
  Что останется: package.json
  Считаем удачей: Хотя бы один следующий PR, трогающий путь Reservation/Queue по времени, ссылается на этот новый скрипт в своём test plan.
  Останавливаемся, если: Если подключение к общему гейту control-plane:check или build заблокирует не связанные с этим PR из-за уже существующего провала -- оставить скрипт отдельным.
- Один Architect-одобренный запуск premvp.command.control_plane_reconcile.v1 (уже PROVEN_PRESENT) закроет разрыв в 36 слитых PR между CURRENT_STATE.yaml и реальным origin/main -- и, отдельно, сверит BLK-001 с уже принятым в CANONICAL_VERTICAL_SPINE_MANIFEST.yaml доказательством прогона 11-12 августа -- без ручного переписывания состояния с нуля.
  Границы: Один запуск, только канонические артефакты control-plane, без изменения roadmap_phase, current_value_step или закрытых блокеров -- решение о том, закрывать ли BLK-001, остаётся за Founder/Architect, не за этим скриптом.
  Что останется: docs/ai-context/control-plane/CURRENT_STATE.yaml
  Считаем удачей: origin_main_sha в CURRENT_STATE.yaml становится проверенным предком текущего origin/main без ручного редактирования JSON, и BLK-001 получает явную ссылку на манифест спины вместо молчания.
  Останавливаемся, если: Если сверка попытается принять любое расхождение продуктовой фазы или закрытых блокеров без Founder/Architect -- остановиться, это решение не для скрипта.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: 11
- стоимость одного проверенного результата: нет данных
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: 3
- создано переиспользуемых артефактов: 12
- ручных сообщений в CloudCode: 0
- ручных сообщений в Codex: 0
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 0 (полнота сбора — неизвестен).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 1 — ежедневный разбор.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Founder подтверждает, что этот консолидированный цикл -- канонический (PR#132 и PR#146 уже закрыты этим запуском), и одобряет один пробный вызов release-конвейера для его слияния. Отдельно: BLK-001 нуждается в сверке с уже принятым доказательством прогона 11-12 августа -- решение за Architect/Founder, не за этим разбором.
