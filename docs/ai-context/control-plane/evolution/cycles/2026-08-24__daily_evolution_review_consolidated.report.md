# Daily Evolution Review

Период: 2026-08-13T19:16:58Z — 2026-08-24T20:14:14Z.

## Главный итог

По бизнесу (запуск, выручка, PnL): продвинулись.
По системе (переиспользуемые возможности): появилась новая переиспользуемая возможность.

Две оценки живут отдельно. Продвижение системы не засчитывается как продвижение бизнеса, и наоборот.

## Ось A — запуск, выручка и PnL

Что сдвинулось:
- Атомарный переход READY -> CLAIMED для строки очереди исполнения устраняет гонку, при которой два одновременных претендента могли выиграть одну и ту же строку Queue -- второй шаг запланированного трёхшагового роллаута New Contour 9.
- Исправлена реальная тихая дыра в обработке callback-исполнений: при отсутствии соответствующей строки очереди handleOrderEventSubmission ранее не возвращал явного исхода; теперь -- явный REJECTED_QUEUE_ROW_NOT_FOUND.
- Исправлена реальная размерностная ошибка: валидация ставки callback ранее сравнивала submitted_size (количество акций) напрямую с queueRow.stake_usd (долларовый потолок) -- некорректное сравнение разных единиц измерения. Теперь ставка в долларах и фактический notional (цена x размер) проверяются раздельно и корректно против долларового потолка очереди.
- Новый модуль учёта жизненного цикла принятых исполнений (fill_status, settlement_status, fee_status, gross_pnl_usd) добавлен и покрыт 179-строчным тестом -- источниковый каркас для будущей сверки, ещё не боевые данные.
- Живой ставочный потолок Tier1 поднят с $1.10 до $2.50 через новый bounded-маршрут R5_CROSS_REPO_OR_LIVE_MONEY. Код слит, тесты проходят; внешнее свидетельство самого Founder-разрешения вне коммита не найдено -- см. not_proven и H3.
- REUSED_FROM_PR165 (период до 2026-08-13T19:16:58Z): New Contour 7 roadmap, резервируемый раннер бэкфилла, индекс для CONTRACT_A_PLANNING_V1, продюсер дедупликации на current-проекции, независимый архитектурный обзор Phase 1.

Какой следующий проверяемый факт в проде стал возможен: Теперь, когда callback-контракт (наличие строки очереди + размерность ставки) исправлен и есть каркас сверки исполнений, следующий проверяемый факт -- реальный ночной прогон живого исполнения с настоящим fill/fee и заполнением executionReconciliation вместо PENDING/null. Это не проверялось в этой сессии (доступ к БД/бирже вне границ)..

Снятые блокеры:
- Тихий проход callback без строки очереди (неопределённое поведение) -- заменён явным REJECTED_QUEUE_ROW_NOT_FOUND.
- Размерностная ошибка в проверке ставки (акции против долларов) -- заменена корректным раздельным сравнением ставки в долларах и фактического notional.
- Гонка двойного захвата строки очереди -- устранена атомарным compare-and-set переходом READY -> CLAIMED.

Сверенный PnL: не заявлен. Без реальных исполнений, комиссий и расчёта он не считается.

## Ось B — Manifest 2

Manifest 2 — это набор переиспользуемых способностей системы: контракты задач, проверяемость, реестр, окружения, права, скрипты, переносимость, восстановление.

Что появилось или окрепло:
- НОВОЕ. Модуль сверки исполнений (executionReconciliation.ts): явные поля fill_status/settlement_status/fee_status/gross_pnl_usd -- первый каркас для сверенного PnL, готовый принять реальные данные fill/fee/settlement. (остаётся в репозитории: lib/executor/executionReconciliation.ts, app/api/executor/order-events/route.ts, scripts/resolve-signals.ts)
- НОВОЕ. Атомарный паттерн compare-and-set для перехода состояния строки очереди (READY -> CLAIMED) -- первый явный пример защиты от гонки конкурентных исполнителей на одной боевой таблице. (остаётся в репозитории: app/api/executor/queue/mark/route.ts, lib/executor/executorCallbackContract.ts)
- НОВОЕ, с оговоркой (см. not_proven). Механизм authorized_bounded_routes для R5_CROSS_REPO_OR_LIVE_MONEY: узкое, ограниченное исключение (сумма, репозиторий, исполнитель, ревьюер) из безусловного BLOCKED для живых денег. (остаётся в репозитории: docs/ai-context/control-plane/ROUTING_AND_PIPELINES.yaml, scripts/control-plane/validate-control-plane.mjs)
- УСИЛЕНО (REUSED domain из PR#165). premvp.reviewer.contur_gate.v1 продолжает вызываться и получать PASS на боевых PR периода (задокументировано явно для PR#167); для PR#168,170,171,172,173 явной цитаты Contur-рецепта в теле PR нет -- см. axis_a/defect_accounting note ниже. (остаётся в репозитории: docs/ai-context/control-plane/reviewers/contur-gate-reviewer.yaml)
- REUSED от PR#165 (изначально PR#154/#132/#124). Mission Contract семантический компилятор. (остаётся в репозитории: docs/ai-context/control-plane/PROMPT__PROTOCOL.md, MISSION_CONTRACT.schema.json, scripts/control-plane/mission-compile.mjs)
- REUSED от PR#165 (изначально PR#135/#146). Навык независимой проверки для нетривиальных миссий. (остаётся в репозитории: .agents/skills/polypropicks-independent-challenge/SKILL.md)

Описание в чате возможностью не считается: нужен артефакт в Git и след его использования или проверки.

## Что доказано

- Атомарный переход READY -> CLAIMED для строки очереди исполнения устраняет гонку, при которой два одновременных претендента могли выиграть одну и ту же строку Queue -- второй шаг запланированного трёхшагового роллаута New Contour 9.
- Исправлена реальная тихая дыра в обработке callback-исполнений: при отсутствии соответствующей строки очереди handleOrderEventSubmission ранее не возвращал явного исхода; теперь -- явный REJECTED_QUEUE_ROW_NOT_FOUND.
- Исправлена реальная размерностная ошибка: валидация ставки callback ранее сравнивала submitted_size (количество акций) напрямую с queueRow.stake_usd (долларовый потолок) -- некорректное сравнение разных единиц измерения. Теперь ставка в долларах и фактический notional (цена x размер) проверяются раздельно и корректно против долларового потолка очереди.
- Новый модуль учёта жизненного цикла принятых исполнений (fill_status, settlement_status, fee_status, gross_pnl_usd) добавлен и покрыт 179-строчным тестом -- источниковый каркас для будущей сверки, ещё не боевые данные.
- Живой ставочный потолок Tier1 поднят с $1.10 до $2.50 через новый bounded-маршрут R5_CROSS_REPO_OR_LIVE_MONEY. Код слит, тесты проходят; внешнее свидетельство самого Founder-разрешения вне коммита не найдено -- см. not_proven и H3.
- НОВОЕ. Модуль сверки исполнений (executionReconciliation.ts): явные поля fill_status/settlement_status/fee_status/gross_pnl_usd -- первый каркас для сверенного PnL, готовый принять реальные данные fill/fee/settlement. — tests/contur3/executionReconciliation.test.ts (179 строк) -- независимо перезапущено вживую в этой сессии, все проверки PASS.
- НОВОЕ. Атомарный паттерн compare-and-set для перехода состояния строки очереди (READY -> CLAIMED) -- первый явный пример защиты от гонки конкурентных исполнителей на одной боевой таблице. — tests/contur3/executorQueueMark.callbackContract.test.ts -- независимо перезапущено вживую, PASS.
- НОВОЕ, с оговоркой (см. not_proven). Механизм authorized_bounded_routes для R5_CROSS_REPO_OR_LIVE_MONEY: узкое, ограниченное исключение (сумма, репозиторий, исполнитель, ревьюер) из безусловного BLOCKED для живых денег. — control-plane:check 250/250 PASS вживую; формально валиден, но единственный экземпляр (FUTURE_QUEUE_STAKE) без независимого свидетельства авторизации -- см. not_proven.
- УСИЛЕНО (REUSED domain из PR#165). premvp.reviewer.contur_gate.v1 продолжает вызываться и получать PASS на боевых PR периода (задокументировано явно для PR#167); для PR#168,170,171,172,173 явной цитаты Contur-рецепта в теле PR нет -- см. axis_a/defect_accounting note ниже. — PR#167 тело PR цитирует 'Contur Gate Review V1: PASS for b46bd389...'.
- REUSED от PR#165 (изначально PR#154/#132/#124). Mission Contract семантический компилятор. — control-plane:check 250/250 PASS, прогнано вживую в этой сессии на текущем origin/main.
- REUSED от PR#165 (изначально PR#135/#146). Навык независимой проверки для нетривиальных миссий. — часть control-plane:check 250/250 PASS, прогнано вживую в этой сессии.

## Что блокирует следующий шаг

- Критерии phase_1_runtime_acceptance по-прежнему не переподтверждены в проде за этот период -- доступ к БД вне границ, повторная проверка не проводилась.
- CURRENT_STATE.yaml.main.origin_main_sha по-прежнему 280d35bd (слияние PR#161) -- живой origin/main теперь на 11 слитых PR впереди (#162,163,164,166,167,168,169,170,171,172,173), заметно больше, чем 3 PR на момент PR#165. STATE_REFRESH_REQUIRED сохраняется и ухудшился.
- EVIDENCE_LEDGER.md по-прежнему заканчивается записью EV-0020 (2026-08-13T05:30:15Z) -- теперь 17 слитых PR (#156-164,166-173) без записи в реестре доказательств, рост с 9 на момент PR#165.
- Это ВОСЬМОЙ подряд PR цикла Evolution (#116,121,122,132,146,154,165 и этот) -- ни один не слит в main. Причина точно локализована вживую: tests/control-plane/evolutionDailyReview.test.mjs:670-673 требует cycles/ без единого .json-файла -- любой реальный цикл проваливает control-plane:check по конструкции теста.
- Единственный след Founder-разрешения на ставку $2.50 и маршрут R5 (authorization_ref FOUNDER_AUTH_R5_PREMVP_FUTURE_QUEUE_STAKE_20260824) -- строка, объявленная и проверяемая тем же коммитом. Независимого артефакта не найдено. Это не значит, что разрешения не было (могло быть вне Git) -- но в репозитории оно не доказано, только самоссылочно.
- PR#149 (проектный документ current-serving модели) по-прежнему не слит.
- tests/contur3/twoStageReservationTiming.test.ts по-прежнему падает на 7 из 14 -- не перепроверялось в этой сессии (не в зоне изменений периода).
- Звено S12 (результат на бирже, callback EXECUTED) остаётся UNPROVEN.
- Сверенный PnL, исполнения, комиссии, расчёт: PR#173 добавил каркас учёта, но ни одного реального значения fill/fee/settlement нет; ничего не заявляется.
- Это ВОСЬМОЙ подряд PR цикла Daily Evolution Review -- и ни один так и не слит в main. В этой сессии впервые точно локализована техническая причина (см. axis_a.not_proven и H1), но сама причина ещё не устранена -- способность 'разбор становится постоянной канонической историей репозитория' остаётся недоказанной восьмой раз подряд.
- premvp.command.release_pipeline.v1 по-прежнему ни разу не был реально вызван ни для одного из восьми циклов Evolution.
- Новый маршрут authorized_bounded_routes для R5 подтверждён только формальной валидацией схемы, не независимым свидетельством самого Founder-разрешения, лежащего в его основе.
- Используются ли компилятор Mission Contract или навык независимой проверки в реальной небоевой задаче исполнителя (не в самом Evolution-разборе), по-прежнему не проверялось.

## Варианты автоматизации

- Отдельная ограниченная миссия (не этот разбор -- ему нельзя менять тесты) обновляет tests/control-plane/evolutionDailyReview.test.mjs:670-673 под реальность Stage 1. Затем один пробный вызов premvp.command.release_pipeline.v1 (validate/dry_run -> resume). — делать сейчас.
  Проблема: Это ВОСЬМОЙ подряд PR цикла Daily Evolution Review -- и ни один не слит в main. В этой сессии впервые вживую воспроизведена точная техническая причина: tests/control-plane/evolutionDailyReview.test.mjs:670-673 буквально требует, чтобы cycles/ содержал ноль .json-файлов -- любой реально сохранённый цикл детерминированно проваливает control-plane:check.
  Что останется в репозитории: tests/control-plane/evolutionDailyReview.test.mjs
  Когда остановиться: остановиться и эскалировать Founder, если обновление теста потребует ослабить любой ДРУГОЙ инвариант control-plane:check помимо этого конкретного цикл-каунта
- Подключить premvp.command.control_plane_reconcile.v1 (уже PROVEN_PRESENT) как обязательный шаг после каждого пакета из 3+ слитых PR, включая одновременное добавление записи EVIDENCE_LEDGER. — делать сейчас.
  Проблема: CURRENT_STATE.yaml отстаёт от live origin/main на 11 слитых PR (#162,163,164,166-173); EVIDENCE_LEDGER.md отстаёт на 17 слитых PR (#156-164,166-173) от своей последней записи EV-0020. Оба разрыва выросли по сравнению с PR#165 (3 и 9 соответственно), несмотря на то что этот же H2 был явно назван в двух предыдущих циклах.
  Что останется в репозитории: docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Когда остановиться: остановиться, если сверка попытается принять расхождение продуктовой фазы без Founder/Architect
- Требовать, чтобы validate-control-plane.mjs проверял НЕ ТОЛЬКО форму authorized_bounded_routes, но и наличие соответствующей отдельной записи в EVIDENCE_LEDGER.md (или новом FOUNDER_AUTHORIZATIONS.md) с тем же authorization_ref, добавленной иначе, чем изменение самого маршрута -- то есть разрешение не может быть тем же коммитом, что и код, который оно разрешает. — делать сейчас.
  Проблема: PR#170 подняло ставку с $1.10 до $2.50 и открыло маршрут R5_CROSS_REPO_OR_LIVE_MONEY через authorization_ref, который тот же коммит и объявляет, и проверяет. Независимого артефакта разрешения нет нигде в репозитории.
  Что останется в репозитории: предлагается: docs/ai-context/control-plane/EVIDENCE_LEDGER.md или новый FOUNDER_AUTHORIZATIONS.md + правило в validate-control-plane.mjs
  Когда остановиться: остановиться, если Founder подтвердит, что текущий уровень доказательства (авторизация вне Git) достаточен и формализация не нужна
- Один комбинированный тест-контракт для lib/executor/executorCallbackContract.ts, покрывающий наличие строки очереди, единицы измерения ставки и предел notional, и переход состояния сверки как один сценарий, а не три отдельных файла. — сначала продукт.
  Проблема: Три последовательных PR на один и тот же файл (executorCallbackContract.ts) за 54 минуты -- каждый закрывает пробел, не покрытый предыдущим (наличие строки очереди -> корректность единиц ставки -> учёт сверки) -- превышает протокольный порог 'два и более LATENT_NEXT_LAYER_EXPOSED на одном пути'.
  Что останется в репозитории: предлагается: tests/contur3/executorCallbackContract.fullBoundary.test.ts
  Когда остановиться: убрать требование, если комбинированный тест окажется избыточным по сравнению с существующими тремя
- REUSED_FROM_PR165 H3/H4 -- зафиксировать общепроектное правило теста формы индекса для любого нового горячего SQL-запроса. — сначала продукт.
  Проблема: REUSED_FROM_PR165 H3/H4. Раскатка current_signal_pair_serving за прошлый период уже показала 8 слоёв за 28 часов; чек-лист готовности горячих SQL-путей (тест формы индекса + бюджет пула + watchdog) до сих пор не формализован как явное требование в docs/ai-context/CLAUDE_CODE_EXECUTION_PROTOCOL.md/VERIFICATION_GATES.md, хотя сам класс отказа 57014 не повторился за этот период.
  Что останется в репозитории: docs/ai-context/VERIFICATION_GATES.md
  Когда остановиться: убрать правило при систематических ложных срабатываниях на холодные запросы
- REUSED_FROM_PR165/PR154 -- требовать в шаблоне PR явную ссылку на статус связанного проектного документа для боевых изменений БД. — сначала продукт.
  Проблема: PR#149 (проектный документ current-serving модели) остаётся неслитым -- теперь ещё дальше от реализации, чем на момент PR#165.
  Что останется в репозитории: предлагается: .github/pull_request_template.md
  Когда остановиться: убрать требование при ложных срабатываниях на намеренно независимые документы
- REUSED_FROM_PR165/PR154 -- если тема PR порождает 3+ отдельных PR за 24 часа, требовать явное поле 'что было упущено в первой версии' в последнем PR серии. — система позже.
  Проблема: REUSED_FROM_PR165 H6. Быстрая переделка одной темы в течение часов теперь наблюдалась снова, дважды в один день: плановый трёхшаговый New Contour 9 (166->167->168, хотя и растянутый на 10 дней между шагом 1 и 2) и трёхслойное ужесточение callback-контракта (171->172->173, за 54 минуты).
  Что останется в репозитории: предлагается: .github/pull_request_template.md
  Когда остановиться: убрать проверку при ложных срабатываниях на намеренно поэтапную разработку

## Две практики Founder

- Распознавать луковичную цепочку (onion chain) вместо серии несвязанных багов -- когда 2+ исправления ложатся на одну и ту же границу за короткое время, останавливаться и спрашивать, какой пограничный инвариант отсутствует, вместо одобрения 'ещё одного точечного патча'.
  Зачем сейчас: Это повторилось СЕГОДНЯ на новой границе: 3 слоя на callback-контракте за 54 минуты (PR#171,172,173), в дополнение к уже описанной 8-слойной цепочке current-serving из PR#165. Паттерн узнаваем и повторяем, а не случаен.
  Как ложится на проект: При просмотре следующей серии из 2+ PR на одну тему за короткое время явно спрашивать: это независимые баги или один и тот же непроверенный пограничный инвариант проявляется слой за слоем? См. H4 выше как готовый пример.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/cycles/2026-08-24__daily_evolution_review_consolidated.json
- Проверять независимое свидетельство перед доверием к самоссылочной авторизации -- когда изменение расширяет чувствительную границу (особенно живые деньги), убеждаться, что разрешение на это существует отдельно от кода, который его использует, а не только как совпадающая строка в том же самом коммите.
  Зачем сейчас: Именно это произошло сегодня: PR#170 расширило риск-класс R5_CROSS_REPO_OR_LIVE_MONEY и подняло ставку до $2.50, но единственное свидетельство разрешения -- authorization_ref, объявленный и проверяемый одним и тем же коммитом. Это не обязательно ошибка, но именно тот случай, когда стоит остановиться и спросить, есть ли независимый след.
  Как ложится на проект: При просмотре любого PR, трогающего R5, платёжные/живые-деньги границы или control-plane governance-файлы (ROUTING_AND_PIPELINES.yaml, validate-control-plane.mjs) одновременно с продуктовым кодом, явно спрашивать: где независимый след разрешения, а не только самосогласованный код? См. H3 выше.
  Что останется в репозитории: docs/ai-context/control-plane/evolution/cycles/2026-08-24__daily_evolution_review_consolidated.json

Сравнение: Первая практика дешева и применяется в одиночку при чтении любого следующего отчёта за пару минут -- достаточно спросить один вопрос про количество слоёв. Вторая требует более внимательного взгляда именно на границы живых денег и control-plane governance, но именно она защищает самую чувствительную часть системы -- поэтому она приоритетнее, хотя обе можно делать параллельно.
Рекомендуемый порядок: сначала P2, затем P1.

## Следующие эксперименты

- Если обновить tests/control-plane/evolutionDailyReview.test.mjs:670-673 (точно локализованный блокер) и слить следующий Evolution-цикл через premvp.command.release_pipeline.v1, впервые за восемь циклов подряд станет известно, работает ли путь черновик -> проверка -> слияние.
  Границы: Один запуск, только обновление этого конкретного теста (не других инвариантов control-plane:check), затем один вызов release_pipeline.v1 в режиме validate/dry_run, потом resume, только для файлов этого PR, без прямого push в main.
  Что останется: tests/control-plane/evolutionDailyReview.test.mjs, docs/ai-context/control-plane/EVIDENCE_LEDGER.md
  Считаем удачей: Тест обновлён, и один PR цикла Evolution слит в main через зарегистрированный конвейер, результат зафиксирован записью EVIDENCE_LEDGER.
  Останавливаемся, если: Если обновление теста потребует ослабить любой другой инвариант control-plane:check, или если конвейер запросит доступ за пределами уже разрешённого -- остановиться и эскалировать Founder.
- Если добавить один комбинированный boundary-тест для executorCallbackContract.ts (наличие строки + единицы ставки + notional + состояние сверки как один сценарий), следующее изменение этого файла не потребует второго PR-исправления в течение того же дня.
  Границы: Один новый тест-файл, применённый только к следующему PR, меняющему executorCallbackContract.ts -- без замены трёх уже существующих тестовых файлов.
  Что останется: предлагается: tests/contur3/executorCallbackContract.fullBoundary.test.ts
  Считаем удачей: Следующее изменение этого файла проходит комбинированный тест и не порождает второй PR-исправление в течение 24 часов.
  Останавливаемся, если: Если комбинированный тест окажется избыточным по сравнению с тремя существующими -- оставить его необязательным образцом, не гейтом.
- REUSED_FROM_PR165 E3, ЕЩЁ БОЛЬШЕ НЕ ИСПОЛНЕНО (разрыв CURRENT_STATE вырос с 3 до 11 PR, разрыв EVIDENCE_LEDGER -- с 9 до 17). Один Architect-одобренный запуск premvp.command.control_plane_reconcile.v1 закроет оба разрыва одновременно.
  Границы: Один запуск, только канонические артефакты control-plane, без изменения roadmap_phase, current_value_step или закрытых блокеров.
  Что останется: docs/ai-context/control-plane/CURRENT_STATE.yaml
  Считаем удачей: origin_main_sha в CURRENT_STATE.yaml становится проверенным предком текущего origin/main, и EVIDENCE_LEDGER.md получает записи для PR#156-173 без ручного редактирования JSON.
  Останавливаемся, если: Если сверка попытается принять любое расхождение продуктовой фазы или закрытых блокеров без Founder/Architect -- остановиться.

## Поддерживающие метрики

Это диагностика, а не оценка. Метрики объясняют вывод, но никогда его не заменяют.

- время до проверенного результата: неизвестно
- доля задач, прошедших с первого раза: неизвестно
- количество переделок: 3
- стоимость одного проверенного результата: нет данных
- отказы ревьюера: неизвестно
- сколько раз получили доказательство из реального рантайма: 3
- создано переиспользуемых артефактов: 3
- ручных сообщений в CloudCode: 0
- ручных сообщений в Codex: 0
- правок от архитектора: 0
- промежуточных действий на одну миссию: неизвестно
- действий на один проверенный результат: неизвестно

Ручных сообщений Founder за период: 0 (полнота сбора — неизвестен).
Правок от архитектора: 0. Они считаются отдельно и в число ручных сообщений не входят.

Полнота сбора неполная, поэтому это нижняя оценка, а не точное число.

## Roadmap

Эволюция системы идёт тремя уровнями: сначала ежедневный разбор, затем управление автоматизацией, дальше — операционная система агентов.
Сейчас: уровень 1 — ежедневный разбор.

Продуктовая фаза, смысл C1 и C2, гейты по PnL и права на реальные деньги этим разбором не меняются.

## Что произойдёт дальше

Founder подтверждает, что этот цикл канонический (PR#165 закрывается как superseded), и рассматривает два приоритета: (1) H1 -- миссия для обновления устаревшего теста-гейта cycles/, чтобы разорвать восьмицикловую цепочку неслитых циклов; (2) H3 -- независимо подтвердить Founder-разрешение на R5 и ставку $2.50 (PR#170).
