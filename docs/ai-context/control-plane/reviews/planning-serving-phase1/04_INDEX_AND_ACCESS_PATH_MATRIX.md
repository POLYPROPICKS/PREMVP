# 04 — Index and Access-Path Matrix

Inventory scope: all indexes materially relevant to `generated_signal_pairs` and `current_signal_pair_serving` found on `origin/main` (`60dbc14`), plus the two externally-referenced indexes named in the mission brief. `git grep` was used repo-wide (not just `supabase/migrations/`) to rule out DDL living elsewhere.

## generated_signal_pairs

| NAME | DEFINITION | PARTIAL_PREDICATE | COLUMN_ORDER | INCLUDE | QUERY_SUPPORTED | QUERY_NOT_SUPPORTED | RUNTIME_STATUS | SOURCE |
|---|---|---|---|---|---|---|---|---|
| `idx_gsp_pending_resolution` | `supabase/migrations/20260702_generated_signal_pairs_pending_resolution_index.sql` | `signal_result IS NULL AND condition_id/selected_token_id/entry_price_num/metric_formula_version IS NOT NULL` | `(expires_at ASC, created_at ASC, id ASC)` | none | Query H-class historical resolver scan (`GSP-H1`) | Query C (wrong leading columns; no identity-tuple prefix) | **CURRENT_INDEX** — present as a migration file, applied per commit history; not independently re-verified against the live catalog in this review | file confirmed on `origin/main` |
| `idx_gsp_shadow_dedup` | `supabase/migrations/20260805_generated_signal_pairs_shadow_dedup_index.sql` | none | `(condition_id, selected_token_id, metric_formula_version)` | none | writer dedup reads (`GSP-W2`); serves as an **identity-prefix match** for query C (impacted_keys join) | Query C's `ORDER BY ... created_at DESC, id DESC` and its `expires_at > now() AND signal_result IS NULL` filter — none of that is in this index, so Postgres must fetch all matching rows and sort them separately (the reported "incremental sort" behavior) | **CURRENT_INDEX** | file confirmed on `origin/main`; migration comment explicitly documents leading-column rationale for 3 *other* call sites, not for query C |
| `idx_gsp_provider_event_context` | `supabase/migrations/20260811_generated_signal_pairs_provider_event_context_index.sql` | `diagnostics->'providerEventContext'->>'v' = 'v1' AND ...->>'provider' = 'polymarket'` | expression index on `(diagnostics->'providerEventContext'->>'eventId', ...->>'eventStartIso')` | none | `GSP-X2` exact-sibling lookup | Query B/C (no identity-tuple columns at all) | **CURRENT_INDEX** | file confirmed on `origin/main` |
| `idx_gsp_planning_scored_rows` | referenced by name in `CURRENT_SIGNAL_PAIR_SERVING_MODEL_DESIGN.md` as "current fix" for `GSP-P1` | UNKNOWN | UNKNOWN | UNKNOWN | legacy `planning_scored_rows_fetch` (`fetchPlanningSourceRowSets`, still live for non-`CONTRACT_A_PLANNING_V1` selector modes) | UNKNOWN | **NOT FOUND AS A MIGRATION FILE** on `origin/main` (`git ls-tree` of `supabase/migrations/` and repo-wide `git grep` both return zero matches) | design doc only; design doc itself flags this as possibly-untracked at time of writing |
| `idx_gsp_planning_shadow_rows` | referenced by name, same design doc, for `GSP-P2` | UNKNOWN | UNKNOWN | UNKNOWN | legacy `planning_shadow_rows_fetch` | UNKNOWN | **NOT FOUND AS A MIGRATION FILE** on `origin/main` | same caveat |
| gameStartIso Planning push-down | referenced in design doc as an index-adjacent predicate fix, not a named index | n/a | n/a | n/a | legacy shadow-rows read | n/a | Locally, the current working tree (this branch, uncommitted) has an in-progress predicate push-down for exactly this (`.not("diagnostics->>gameStartIso", ...)`) — **not on `origin/main`**, and unrelated to this review's authority scope; noted only to avoid confusing it with a claimed-applied fix | uncommitted local diff, out of authority per mission instructions |
| `idx_gsp_current_serving_backfill_order` | supplied production evidence: "created/recovered and runtime-proven valid/ready"; `CURRENT_STATE.yaml.current_serving_authority.index_recovery`: `"EXTERNALLY_PROVEN_COMPLETE; idx_gsp_current_serving_backfill_order indisvalid=true indisready=true; not revisited or modified."` | UNKNOWN (no DDL source found) | UNKNOWN | UNKNOWN | presumed: query A (eligible backfill selector) | UNKNOWN | **RUNTIME_PROVEN** per supplied/recorded evidence only — **HISTORICAL/UNSOURCED**: zero matches for this name anywhere in `origin/main` or the local `.worktrees/` copy (`git grep`, repo-wide, 0 hits) | `CURRENT_STATE.yaml` + `EVIDENCE_LEDGER.md` EV-0020 only; **no migration file exists for this index anywhere accessible to this review** |
| `idx_gsp_current_serving_latest_unresolved` (Codex-proposed) | proposed migration path named in the mission brief: `supabase/migrations/20260813094706_current_signal_pair_serving_latest_active_index.sql` | proposed: `WHERE signal_result IS NULL AND identity columns non-null` | proposed: `(condition_id, selected_token_id, metric_formula_version, created_at DESC, id DESC)` | none proposed | would directly cover query C's join+filter+sort in one index | n/a | **PROPOSED_NOT_APPLIED** — production DDL apply did not complete (~100s gateway timeout, per mission brief) | **File not found anywhere on this machine** — not on `origin/main`, not in `.worktrees/`, not found by filesystem search for the filename or timestamp prefix. This review cannot independently confirm the proposed DDL's exact text; it is recorded here as an **unverified claim from the mission brief**, not a corroborated artifact. |

## current_signal_pair_serving

| NAME | DEFINITION | QUERY_SUPPORTED | RUNTIME_STATUS |
|---|---|---|---|
| Primary key `(condition_id, selected_token_id, metric_formula_version)` | `supabase/migrations/20260812_current_signal_pair_serving.sql:21` | ON CONFLICT target (query D); would also serve point lookups by full identity | **CURRENT_INDEX** (implicit PK index) |
| Unique `(source_generated_signal_pair_id)` | same file, line 22 | lineage-uniqueness enforcement; not used by any read query in files 03 | **CURRENT_INDEX** |
| Any index supporting queries F/G's predicate (`projection_status`, version, `signal_result`, `expires_at`, `signal_confidence_num`, ordered by `source_created_at DESC, source_generated_signal_pair_id DESC`) | none found | Contract A Planning serving reads (queries F, G) | **NOT FOUND** — `UNKNOWN` whether this matters in practice (table is expected to stay small/bounded by design, unlike the historical corpus), but this was not measured. Flag for the reviewer: if `current_signal_pair_serving` itself grows large (e.g. if expired/resolved rows are never pruned — no pruning job was found in source), query F/G could eventually face the same class of problem the whole migration exists to avoid. |

## Summary classification

- **CURRENT_INDEX** (present as migration file on `origin/main`, historically-attributed to a documented production incident): `idx_gsp_pending_resolution`, `idx_gsp_shadow_dedup`, `idx_gsp_provider_event_context`, plus the two `current_signal_pair_serving` implicit indexes.
- **HISTORICAL / NOT SOURCE-VERIFIABLE, PRODUCTION-CLAIMED VALID**: `idx_gsp_current_serving_backfill_order`.
- **PROPOSED_NOT_APPLIED, UNVERIFIED ARTIFACT**: `idx_gsp_current_serving_latest_unresolved` — the mission's central open question (should this be created) cannot even be checked against its own claimed source file, because that file does not exist on any branch or worktree accessible to this review.
- **NAMED IN DESIGN DOC, NOT FOUND AS MIGRATIONS**: `idx_gsp_planning_scored_rows`, `idx_gsp_planning_shadow_rows`.

This gap — a production-critical index whose DDL is not in version control at all (`idx_gsp_current_serving_backfill_order`), and a proposed index whose DDL file is referenced by exact path but does not exist anywhere this review could reach (`idx_gsp_current_serving_latest_unresolved`) — is itself evidence of a process gap independent of which architecture the reviewer picks. See file 06.
