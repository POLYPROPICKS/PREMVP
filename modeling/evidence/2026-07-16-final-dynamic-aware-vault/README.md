# Final Dynamic-aware Vault frontier

Bounded historical pseudo-out-of-sample comparison of exactly seven predeclared arms. It preserves the locked PRIMARY, 231 intended IDs, Dynamic Active 3% stake schedule, 24×7 eligibility, 36-position limit, 100% Active exposure ceiling, entry ordering and settlement batching.

The locked confirmation result is `NO_DYNAMIC_VAULT_PASSED_PREDECLARED_GATES`; `DYNAMIC_NO_VAULT` remains the winner. No further candidate round is authorized.
