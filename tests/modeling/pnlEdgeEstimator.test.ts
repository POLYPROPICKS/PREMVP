import test from "node:test";import assert from "node:assert/strict";import {estimatePastOnlyPnlEdge} from "../../lib/modeling/pnlEdgeEstimator";
test("empirical Bayes is past-only, stable and deterministic",()=>{const history=Array.from({length:40},(_,i)=>({observationId:String(i),resolvedAtMs:i<20?50:150,win:i%3!==0,score:70,price:.5,coverage:80,marketFamily:"winner"}));const d={observationId:"d",decisionAtMs:100,score:70,price:.5,coverage:80,marketFamily:"winner"};const a=estimatePastOnlyPnlEdge(d,history);const b=estimatePastOnlyPnlEdge(d,[...history].reverse());assert.deepEqual(a,b);assert.equal(a.sampleSize,20);assert.ok(Number.isFinite(a.posteriorStdDev));assert.ok(a.posteriorWinProbability>0&&a.posteriorWinProbability<1)});
test("hierarchy backs off deterministically",()=>{const d={observationId:"d",decisionAtMs:100,score:99,price:.5,coverage:1,marketFamily:"x"};const a=estimatePastOnlyPnlEdge(d,[],20);assert.equal(a.bucketLevel,"GLOBAL_PRIOR");assert.equal(a.posteriorWinProbability,.5)});
