import test from "node:test";import assert from "node:assert/strict";import {bootstrapMinskNights,replayNightStakeArchitecture} from "../../lib/modeling/nightStakeArchitecture";import type{ExecutionCandidate}from"../../lib/modeling/executionWaterfall";
const c=(id:string,decision:string,resolved="2026-07-16T08:00:00Z"):ExecutionCandidate=>({observationId:id,identity:id,matchKey:id,decisionAtIso:decision,createdAtIso:"2026-07-15T00:00:00Z",resolvedAtIso:resolved,finalScore:70,dataCoverage:80,entryPrice:.5,row:{id,signal_result:"won"}});const mapping={"0.30_0.40":1,"0.40_0.50":1,"0.50_0.60":1,"0.60_0.70":1,"0.70_1.00":1}as const;
test("allocation is order-independent and every row has one terminal reason",()=>{const rows=[c("a","2026-07-15T15:00:00Z"),c("b","2026-07-15T15:00:00Z")],cfg={referencePolicy:"MINSK_NIGHT_FIXED_MAX3_V1"as const,operationWindowOnly:false,priceBandMapping:mapping,maxOpenPositions:30,maxOpenExposurePct:.8,maxAcceptedPerUtcDay:100,initialActiveBankroll:50};const a=replayNightStakeArchitecture(rows,cfg),b=replayNightStakeArchitecture([...rows].reverse(),cfg);assert.deepEqual(a,b);assert.equal(a.rows.length,2);assert.ok(a.meanStake<=a.rows[0].maximumStake)});
test("Minsk-night bootstrap is reproducible",()=>{assert.deepEqual(bootstrapMinskNights({a:2,b:-1}),bootstrapMinskNights({a:2,b:-1}))});
